# Gather Web — Epic Roadmap

**Status:** Living, working document — update directly as phases progress, same convention as the Salesforce-era `gather_frontend_epic_plan.md` this sequencing mirrors. Companion to `gather_web_architecture.md` (stack/data-model/RLS reference; the dated engineering log for completed phases lives there, not here) and `gather_web_user_journeys.md` (the narrative each phase is making real).

## Context

`gather_web_architecture.md` already ports the Salesforce ERD, RLS/authorization model, and a 10-phase sequence that mirrors the Salesforce-era 10-epic frontend plan almost 1:1. What that doc's Build Phases table didn't have — and what made the Salesforce-side `gather_frontend_epic_plan.md` genuinely useful during that build — was per-phase detail beyond one sentence, a concrete kickoff sequence for whatever's next, and a running Outstanding Items checklist tagged by the phase that surfaced each item. This doc adds all three, and is expected to be edited every session the way the Salesforce doc was.

Phases 0 through 9 — the entire originally-planned consumer-facing roadmap — are done, built and verified against a real local Supabase stack. See `gather_web_architecture.md`'s "Local environment setup log" for the full dated account. **Phase 10 (Admin & Support Console, see `gather_web_admin_architecture.md`) is the only phase left.** A handful of items remain genuinely open by necessity, not oversight — see Outstanding Items: two need Andre directly (a real mobile device test, a Stripe account), one needs a design decision (the OKLCH palette's measured contrast failures). The long-deferred background-job decision for payment reminders was resolved a different way (2026-09-16) — see Progress Log — by sending reminders implicitly instead of on a schedule, deliberately bypassing rather than making the Edge-Functions-vs-Inngest choice.

---

## Epic / Phase Breakdown

| # | Phase | Contents | Depends on |
|---|---|---|---|
| 0 | Scaffolding | Next.js app, Supabase project, Drizzle schema + migrations for the full 15-table set, RLS policies for the owner/guest baseline (`events`, `event_collaborators`, `vendors`, `vendor_team_members`, `event_vendors`), Supabase Auth wired, Vercel deploy of an empty shell. | — |
| 1 | Design system | Tailwind theme from the OKLCH tokens, `next/font` Fredoka+Nunito, base primitives (`button`, `input`, `card`) matching the prototype's pill/2px-border language. | 0 |
| 2 | App shell & cross-cutting state | Root layout, bottom tab bar (auth/persona-aware — guest vs. planner vs. vendor tab sets, per the architecture doc's Screen/Route Inventory), a session-context hook/Server Component reading Supabase Auth state, a persona-switcher store (Zustand — replaces LMS's `PersonaContext`, fixing the documented late-subscriber bug by using reactive state instead of a one-shot publish), a toast system. | 0, 1 |
| 3 | Guest & public browse | `/`, `/vendors`, `/vendors/[id]`, guest RSVP on `/events/[id]`. Exercises the `anon`-role RLS policies first — same "riskiest, least locally-testable part first" sequencing the Salesforce plan used for its Guest User Sharing Rules. `/` must branch on auth state here (never done in the LWC build — tracked as a real gap it must close, not repeat). | 2 |
| 4 | Registration & onboarding | Supabase Auth signup (`/register`), planner onboarding (`/onboarding/planner`), vendor onboarding (`/onboarding/vendor`) — this is also where the vendor self-registration → Owner path gets its real UI (the RLS/DB side of this was already proven working in the Phase 0 verification). | 2 |
| 5 | Planner core: events | `/events`, `/events/new`, `/events/[id]/edit`, `/events/[id]/attendees`, `/events/[id]/tasks` — events CRUD, the multi-step wizard, attendee/task sub-sections, collaborator invite/accept. Needs RLS policies added for `event_attendees` and `event_tasks` (currently service-role-only). | 4 |
| 6 | Vendor directory & claims | `/vendors/add` (search-first, dedupe-gated stub creation — see Business Rules & Invariants), `/vendors/[id]/claim`. Needs RLS policies added for `vendor_services` and `vendor_claim_requests` (currently service-role-only), plus the `approveClaim` Server Action that must auto-reject competing pending claims in the same transaction. | 3, 4 |
| 7 | Commerce | `/events/[id]/vendors` (quote review, accept/decline — backend enforces one-Accepted-at-a-time, frontend just refreshes), `/events/[id]/payments` (payment plan + installments, Stripe integration). Needs RLS policies for `vendor_quotes`, `payment_plans`, `payment_installments`, `payment_reminders` (all currently service-role-only) plus the installment-sum-≤-plan-total constraint wired in. | 5, 6 |
| 8 | Vendor persona | `/vendor/[vendorId]/dashboard` (bookings via a live join — the direct fix for the Salesforce-era "snapshot at association time" team-visibility bug — quote submission gated to Manager+, service listings), `/vendor/[vendorId]/team` (invite/role-change/revoke, Owner-only). | 4, 2 |
| 9 | Polish & hardening | Notification-preferences schema (never existed — a real schema decision, not a quick add), `Intl.NumberFormat` currency formatting everywhere money renders, an accessibility pass, Vitest/RTL coverage across all phases (from the start this time, not deferred). | all |
| 10 | Admin & support console | A separate `/admin/*` namespace for internal staff — see `gather_web_admin_architecture.md` for the full design. `profiles.is_admin` + a `SECURITY DEFINER` `is_admin()` function gate every admin route via `requireAdmin()`, then a service-role client (not per-table RLS policies) handles all data access. New tables: `support_cases`, `support_case_comments`, `admin_audit_log`. `support_case_comments`/`admin_audit_log` stay service-role-only; `support_cases` gained two real self-service RLS policies later (2026-09-16, self-service case submission — see Progress Log) so a planner/vendor can log and view their own cases directly, not just through this console. Subsumes the admin claim approve/reject screen originally left undecided in Phase 3/6/9 — it ships here (`/admin/vendors/claims`) instead. Deliberately not mobile-first, unlike every consumer-facing phase — its own desktop-oriented layout, no shared AppShell. | 6 (for claims data to review) |

---

## Phase 2 Kickoff Sequence (completed 2026-09-13)

Kept as a historical record of what was planned vs. what actually happened — see `gather_web_architecture.md`'s engineering log for the two real bugs found along the way (a cookie-mutating Server Action discarding its own return value, and why the persona store needed to be per-request rather than a global singleton).

1. Root layout (`src/app/layout.tsx` already exists from Phase 1 — extend it, don't replace it) gets a persistent bottom tab bar component, initially guest-only (Home / Vendors / Log In), matching the Screen/Route Inventory's tab sets.
2. A session-context source of truth: a small server-side helper (using `src/lib/supabase/server.ts`, already built in Phase 0) that reads the current Supabase Auth session and profile row, callable from both Server Components and Server Actions — the direct replacement for `SessionContextController.getSessionContext`, and deliberately **not** a one-shot context published only at shell-mount (that's the exact bug being fixed).
3. A Zustand persona-switcher store, seeded from the session helper's vendor-team memberships — no UI yet, just the store and its shape, so Phase 4/8 can build against a stable interface.
4. A minimal toast system (a Zustand store or React Context + a fixed-position renderer in the root layout) — no dependency on any feature work, safe to build in isolation and reuse everywhere.
5. Wire the tab bar to the session helper so it swaps between guest/planner/vendor tab sets live — this is the first real end-to-end proof that Phase 2's session plumbing works, the same role `PublicEventController.getPublicEvents` played as the "first real wire call" in the Salesforce kickoff sequence.
6. Verify in a real browser per the architecture doc's Verification section (not just `next build`) — sign in as a real local Supabase Auth user created via Phase 0's verification flow, confirm the tab set actually changes, confirm zero console errors, check the fixed bottom tab bar against iOS Safari's safe-area insets if a physical device or simulator is available.

Done exactly as planned, steps 1-5. Step 6 was verified with Playwright against a real dev server and a real local Supabase Auth user (guest → login → authenticated tab set → profile → logout → guest tab set again, zero console errors) — the one piece **not** yet checked is real iOS Safari safe-area-inset behavior, since that requires a physical device or simulator, neither available in this pass; carry that check forward to whenever a device is available, per the architecture doc's mobile-first testing requirement.

## Phase 3 Kickoff Sequence (completed 2026-09-13)

Kept as a historical record — see `gather_web_architecture.md`'s engineering log for what was actually verified and the vendor-visibility gap found along the way.

1. Replace `src/app/page.tsx`'s Phase 1 style-proof content with the real guest Home: live public events + a verified-vendor teaser, branching on auth state (session is already available via the root layout — this closes the "guest home doesn't branch on auth state" item that was never fixed once across the whole Salesforce LWC build).
2. Replace the `/vendors` placeholder with the real guest-browsable directory: category filter + keyword search against the `anon`-role RLS policy on `vendors` (`verification_status = 'verified'` — see Authorization Model Port).
3. Build the guest RSVP path on `/events/[id]` (a route that doesn't exist yet at all) as a Server Action with a narrow, explicit input shape using the Supabase service role — per the Authorization Model Port's "guest writes" rule, never a client-side `anon` INSERT policy on the raw table. Real validation from the start (the Outstanding Items list below flags the Salesforce build's bare `if (!name || !email)` check as a known gap not to repeat).
4. Verify against the real local stack per the Verification section: an anonymous request/browser session should see published events and verified vendors, and be able to RSVP without any account — the same "riskiest, least locally-testable part first" sequencing this phase was chosen for.

Done exactly as planned, all four steps — see `gather_web_architecture.md`'s engineering log for the full account, including the RSVP write's narrow-DTO design and a real, currently-dormant gap found in the vendor-visibility RLS policy (see Outstanding Items below).

## Phase 4 Kickoff Sequence (completed 2026-09-13)

Kept as a historical record — see `gather_web_architecture.md`'s engineering log for what was actually verified and the two real Router-Cache/derived-state bugs found proving the vendor path.

1. Build `/register` (Supabase Auth signup via a Server Action, following the same pattern `/login` already established — success redirects server-side, per the cookie-mutation/remount lesson from Phase 2) with a `persona` query param (`?persona=planner|vendor`) so Home's two hero CTAs (currently both pointing at `/login` as a Phase 3 placeholder) can finally point somewhere real.
2. Build `/onboarding/planner` (name/phone → `profiles`) and `/onboarding/vendor` (business name/category/description/phone/website → a new `vendors` row, registrant becomes Owner in `vendor_team_members` in the same transaction) as the continuations after signup.
3. Confirm the vendor self-registration → Owner chicken-and-egg path works through the real UI, not just the Phase 0 API-level proof — this is the single most consequential thing the Salesforce build never resolved (it was genuinely unknown whether a Customer Community User could create an Account at all), so seeing it work end-to-end through an actual form is worth calling out explicitly when it happens.
4. Update Home's hero CTAs and the guest tab bar's "Log In" tab to link to the new real routes; keep `/login` itself as the sign-*in* page (already correct) rather than folding it into `/register`.

Done exactly as planned, all four steps — plus a real schema gap found and fixed along the way (`profiles` had no `phone` column, needed for planner onboarding) and two subtle Router-Cache/derived-state bugs that only surfaced testing the vendor path through the actual UI. See `gather_web_architecture.md`'s engineering log and `teachAndre/04-derived-state-and-router-cache.md` for the full account.

## Phase 5 Kickoff Sequence (completed 2026-09-13)

Kept as a historical record — see `gather_web_architecture.md`'s engineering log for the three real bugs found (an ambiguous PostgREST embed silently swallowed as an empty list, a real RLS gap hiding a pending invitee's event name, and a migration-tracking lesson about never editing an already-applied Supabase CLI migration file).

1. Add RLS policies for `event_attendees` and `event_tasks` (currently service-role-only, per Phase 0's scope boundary) — owner/collaborator read-write, matching the `events` table's own owner-or-editor-collaborator shape.
2. Replace `/events`'s Phase 2 placeholder with the real My Events list (`EventController.getMyEvents` equivalent — a straightforward `owner_id = auth.uid()` query, RLS does the filtering).
3. Build `/events/new` and `/events/[id]/edit` — the event creation/edit wizard. Timezone shown read-only as SAST per the ZAR/SAST-only launch principle, matching the Salesforce build's own choice not to build a picker nobody in this market needs yet.
4. Extend `/events/[id]` (currently guest-only from Phase 3) with the owner/collaborator management branch — an Edit entry point, plus `/events/[id]/attendees` and `/events/[id]/tasks` sub-sections — rather than rebuilding the route from scratch.
5. Build collaborator invite/accept (`event_collaborators`) — this is what a second planner needs to help manage an event, and it's the first real exercise of RLS policies that already exist from Phase 0 but have never been driven through a UI (`events_select_owner_or_collaborator`, the `event_collaborators` policies).

Done exactly as planned, all five steps — plus one deliberate simplification (a single-form event wizard instead of the Salesforce build's 4-step one, since a mobile-first Next.js page doesn't need the same pacing an LWC screen did) and a new RLS policy neither this plan nor Phase 0 anticipated (`events_select_pending_invitee`), found only by actually testing the accept-invite flow end to end.

## Phase 6 Kickoff Sequence (completed 2026-09-13)

Kept as a historical record — see `gather_web_architecture.md`'s engineering log. Notably the first phase in a while with no new bug found; the work reused already-proven patterns closely enough (search-first flow, form shapes, a straightforward policy widening) that nothing new broke.

1. Add RLS policies for `vendor_services` and `vendor_claim_requests` (currently service-role-only) — services readable by anyone (they're part of a vendor's public profile), writable by Owner/Manager team members; claim requests visible to their claimant and to the vendor's existing team, insertable by any authenticated user.
2. Decide and resolve the dormant vendor-visibility gap flagged back in Phase 3: `vendors_select_public_or_own` only lets `anon` see `verification_status = 'verified'` vendors, but the documented bootstrapping workflow calls for unclaimed stubs to appear in the public directory too, marked Unverified. This phase is the one actually creating stubs, so the policy needs to change now, not stay dormant further.
3. Build `/vendors/add` — search-first (reusing `/vendors`' existing search), stub creation only after confirming no match, per the dedupe-before-create business rule.
4. Build `/vendors/[id]/claim` — a claim request form on the vendor profile page, shown when `verification_status !== 'verified'`, using the same `findUserByEmail`-style "resolve identity narrowly" pattern established in Phase 5 where relevant.
5. No admin approve/reject UI to decide on here anymore — resolved by adding Phase 10 (Admin & Support Console, see `gather_web_admin_architecture.md`), which owns that screen (`/admin/vendors/claims`). This phase only needs claims to actually exist and be submittable; reviewing them is Phase 10's job.

Done exactly as planned, all five steps — including updating `/vendors` itself to actually show unclaimed/claim_pending vendors (badged Unverified), not just widen the RLS policy without the directory page using it.

## Phase 7 Kickoff Sequence (completed 2026-09-13)

Kept as a historical record — see `gather_web_architecture.md`'s engineering log. One correction found while executing step 2/3 below: the one-Accepted-quote and installment-sum invariants were only ever *documented* intent in the Business Rules table, not actually built into the schema yet — this phase is the one that actually built both (a partial unique index and a trigger, respectively), not just added RLS on top of already-enforced invariants as originally assumed.

1. Add RLS policies for `vendor_quotes`, `payment_plans`, `payment_installments`, and `payment_reminders` (currently service-role-only) — scoped through `event_vendors` back to the event owner/editor-collaborator on the planner side, and through `vendor_team_members` (Manager+) on the vendor side, mirroring the two-sided access shape `event_vendors` itself already has.
2. Build `/events/[id]/vendors` — list `event_vendors` for the event, quote review, accept/decline. The one-Accepted-quote-at-a-time invariant (partial unique index, already in the schema) means the frontend just refreshes after either action rather than re-implementing that rule client-side.
3. Build `/events/[id]/payments` — payment plan creation, installments, marking paid. The installment-sum-≤-plan-total constraint is likewise already enforced at the DB level; don't duplicate a running-total check in the form.
4. Decide the Stripe integration's actual shape before wiring it in: at minimum, which side initiates a payment (planner marking an installment paid manually vs. a real checkout flow), and whether Phase 7 ships real Stripe test-mode charges or defers to a later hardening pass — `payment_installments` already reserves `payment_gateway`/`gateway_transaction_id`/`gateway_status` columns for this, so the schema isn't the blocker, the integration surface is.
5. There's no UI yet for a vendor to *submit* a quote in the first place (`VendorQuoteController.submitQuote`'s equivalent) — that's Phase 8's vendor dashboard, matching how the Salesforce build split planner-side commerce (Epic 7) from vendor-side quote submission (Epic 8). Phase 7 only needs to seed a quote via the service role or Studio for testing the planner-side accept/decline flow, not build the submission form itself.

Done, all five steps, plus one thing this plan didn't anticipate needing: there was no way to associate a vendor with an event at all until this phase added it to `/vendors/[id]` — without it, none of the pages built here would have been reachable through the product itself. Stripe was deliberately deferred per step 4's own framing — manual tracking is documented as the current intentional state (ERD Design Principle 9), and real integration needs Andre's own Stripe account, the same class of blocker as the still-pending Vercel login.

## Phase 8 Kickoff Sequence (completed 2026-09-13)

Kept as a historical record — see `gather_web_architecture.md`'s engineering log for the new `auth.users`-reading RLS helper this phase needed and the Profile-query subtlety it surfaced.

1. Build `/vendor/[vendorId]/dashboard` (replacing the Phase 2 placeholder) — bookings (events that have associated this vendor, the reverse direction from `/events/[id]/vendors`), a quotes inbox, and quote *submission* (`vendor_quotes` INSERT — the RLS policy for this already exists from Phase 7, gated to Owner/Manager, unexercised by any UI yet).
2. Build `/vendor/[vendorId]/team` — invite/role-change/revoke for `vendor_team_members`, plus resolving `vendor_team_invites` (an email-based bridge, mirroring `event_collaborators`' invite/accept shape from Phase 5 — likely needs its own small RLS policy set, not yet added).
3. Wire up service-listing management (`vendor_services` — RLS already added in Phase 6, no UI yet) from the vendor dashboard.
4. Fix the "snapshot at association time" sharing bug for real this time: confirm `event_vendors_select_event_side_or_vendor_side`'s live `is_vendor_team_member` join actually means a teammate added *after* a booking exists can still see it — this was designed correctly back in Phase 0 but never exercised through a real multi-person team until this phase's dashboard gives a reason to click through it.
5. Full persona-switcher exercise: a user holding both a Planner identity and a Vendor persona for the vendor they're now managing — confirm switching between them via the Phase 2 persona switcher correctly changes which tab set and dashboard they see, the first time both personas' real screens exist simultaneously to switch between.

Done, all five steps — plus one thing step 2 flagged as "likely needs its own RLS policy set" turned out to need more than expected: `vendor_team_invites` is email-keyed, not `user_id`-keyed like every other invite table in this schema, which needed a genuinely new kind of helper function (reading `auth.users`, not just `public.*` tables). Step 4's live-join fix and step 5's persona switch were both proven together in one continuous test: a teammate invited before a booking existed, who then created that exact booking themselves (in their Planner persona) before ever accepting the vendor invite, then switched personas and saw it immediately.

## Phase 9 Kickoff Sequence (completed 2026-09-13, with two items honestly still open)

Kept as a historical record — see `gather_web_architecture.md`'s engineering log for the full account, including the real (not eyeballed) WCAG contrast measurements and why they're flagged rather than silently fixed.

1. Design and build a real notification-preferences schema — never existed at all, a genuine schema decision (which channels, per what event type, opt-in vs. opt-out defaults) rather than a quick add. Decide scope before writing migrations: at minimum, does this phase also need the background-job infrastructure for `payment_reminders` (Supabase Edge Functions + `pg_cron`, or Inngest — still an open stack decision), or does it stop at the preferences schema and defer actually sending anything?
2. An accessibility pass across all eight built phases — this project has had zero dedicated a11y review so far; screen-reader labels, focus states, color contrast (verify the OKLCH token palette actually meets WCAG AA, not just "looks fine"), and keyboard navigation through every form built.
3. Vitest + React Testing Library setup — genuinely from scratch, since it was deliberately not deferred phase-by-phase but also never actually started either. Decide realistic coverage scope for a "polish" pass: likely the shared primitives (`Button`/`LinkButton`/`Card`/`Input`) and the highest-risk Server Actions (invariant-adjacent ones: accept-quote, add-installment) rather than attempting full coverage retroactively across nine phases in one pass.
4. A real mobile-device pass — per the Verification section's own standing requirement, this project has been verified almost entirely through headless Chromium screenshots so far, not an actual phone. `npm run dev:mobile` + a real device, checking the fixed bottom tab bar against iOS Safari's safe-area insets specifically, which no automated check in this project has touched yet.
5. Re-audit every `Outstanding Items` entry below before considering Phase 9 done — this is the designated catch-all phase for anything still open, not just its own four bullet points above.

**What actually happened, honestly, per step:**
1. Done — `notification_preferences` table + Profile UI, scoped to stop at preferences (background-job infra stays open, tracked in Outstanding Items).
2. Partially done — a systemic fix (Input's placeholder → aria-label derivation, a focus-visible ring on every Button/LinkButton) reaches every form in the app at once via the shared primitives, plus real, computed (not eyeballed) WCAG contrast numbers for the whole palette. **Not done**: a per-page audit of all ~20 routes, or fixing the contrast failures the measurement found — see Outstanding Items.
3. Done, but scope-limited by a real Next.js constraint discovered while doing it: Vitest doesn't support async Server Components at all (confirmed in Next's own testing docs), and every `page.tsx` in this project is one — so this suite covers shared UI primitives and pure utilities only, not pages or Server Action business logic. 16 tests, all passing.
4. Partially done — real device *presets* (correct viewport/touch/pixel-ratio via Playwright) caught one thing worth checking twice (a false-positive tab-bar overlap, confirmed harmless by scrolling to the actual bottom) but Chromium still can't validate real `env(safe-area-inset-*)` behavior. **Not done**: an actual physical device test — still needs Andre.
5. Done — see the two new Outstanding Items below (contrast failures, real-device gap) plus the two long-open items (background jobs, physical device) carried forward rather than closed by assumption.

---

## Outstanding Items

A running checklist, tagged by the phase that surfaced or should close each item — kept separate from the Progress Log below so it stays scannable, same convention as the Salesforce-era doc. Nothing here blocks the current phase; each is either a deferred-by-design scope boundary or a real gap worth closing later.

- [x] **Guest home (`/`) doesn't branch on auth state.** Never done once in the Salesforce LWC build — fixed in Phase 3: an authenticated visitor sees a personalized welcome and quick links instead of the guest hero CTAs, both variants sharing the same live events/vendors content beneath. *(Phase 3.)*
- [x] **RSVP form needs real validation**, not a bare `if (!name || !email)` check — the Salesforce build shipped exactly that and flagged it as worth tightening later. Done properly in Phase 3: zod field-level validation server-side (name length, email format, guest-count bounds) plus native HTML5 validation client-side, both actually exercised and verified, not just written. *(Phase 3.)*
- [x] **URL should reflect the current tab/view.** Resolved by construction in Phase 2 — the tab bar links to real routes (`/`, `/vendors`, `/events`, `/profile`, `/vendor/[id]/dashboard`) and highlights active state from `usePathname()`, so the URL is always the source of truth for the current view, unlike LWR's unverified `NavigationMixin` behavior the Salesforce build never resolved. *(Phase 2.)*
- [x] **Vendor team "snapshot at association time" sharing bug — fix, don't repeat.** The Salesforce build documented this as "not fixable from the frontend": a teammate added to a vendor's team after a booking (`event_vendors` row) already existed wouldn't see it, because the sharing grant was snapshotted, not live. Not just confirmed by reading the policy — actually proven in Phase 8 with a real test: a teammate was invited, a booking was created for that vendor *before* they accepted, and the dashboard showed it to them immediately upon accepting. *(Phase 8 — proven, not just assumed.)*
- [x] **Money must render via `Intl.NumberFormat`, never string concatenation.** The Salesforce build rendered bare `R{amount}` everywhere commerce data appeared and never fixed it across 3 epics. Done in Phase 7: `formatZAR()` (`src/lib/utils.ts`) built once and used from the first commit that rendered any money at all. Confirmed reused correctly (not reinvented) in Phase 8's vendor dashboard, the second place money renders — the helper held up across both phases. *(Phase 7, confirmed in Phase 8.)*
- [x] **Admin claim approve/reject needs an actual screen.** Salesforce deferred this entirely to a Setup list view — gather-web has no equivalent free admin surface, so this was flagged as net-new work rather than a straight port. Resolved by scoping a full Phase 10 (Admin & Support Console, see `gather_web_admin_architecture.md`) rather than a one-off screen bolted onto Phase 6 or 9 — shipped 2026-09-14 as `/admin/vendors/claims`, verified end to end (approve provisions Owner + verifies the vendor + auto-rejects competing claims). *(Phase 10.)*
- [x] **Notification/reminder preferences have no schema at all.** Never existed on the Salesforce side either (flagged there as "a schema decision, not a quick add"). Done in Phase 9: `notification_preferences` (one row per user, upserted on first save rather than seeded by a trigger), an email-reminders toggle wired into Profile, SMS/push left honestly disabled ("coming soon") rather than pretending those channels work. *(Phase 9.)* Actually *sending* reminders shipped 2026-09-16 — see below, no longer open.
- [~] **Vitest + React Testing Library coverage from the start.** The single biggest process gap the Salesforce frontend build called out in hindsight ("no LWC Jest tests were written across any of the ten epics") — the explicit intent this time was tests landing alongside each phase's components/Server Actions, not deferred to Phase 9 as a catch-up pass. **Honest assessment: that intent wasn't met** — no test infrastructure existed before Phase 9, meaning this repeated the exact anti-pattern it warned against for 8 phases. Phase 9 did set up real infrastructure (Vitest + RTL + jsdom, 16 passing tests) covering the shared UI primitives and pure utility functions — but async Server Components (every `page.tsx` in this project) aren't supported by Vitest at all per Next.js's own testing guide, so this is a smaller foundation than "full coverage," not a completed catch-up. Server Action business logic (zod schemas, RLS-dependent behavior) remains covered only by the manual Playwright pass each phase has gotten, not by this suite. *(Should have started Phase 2; actually started Phase 9.)*
- [x] **RLS policies for `event_attendees` and `event_tasks`** — done in Phase 5, owner-or-any-accepted-collaborator to read, owner-or-Editor to write, matching `events` itself. *(Phase 5.)*
- [x] **RLS policies for the remaining 7 tables** (`vendor_services`, `vendor_claim_requests`, `vendor_team_invites`, and the full commerce block) — all done: `vendor_services`/`vendor_claim_requests` in Phase 6, the full commerce block in Phase 7, `vendor_team_invites` in Phase 8. Every table in the schema now has real policies except `support_case_comments`/`admin_audit_log` (deliberately service-role-only per `gather_web_admin_architecture.md`, since a case's comment thread and the audit log can both carry internal-only content). `support_cases` and `payment_reminders` are both partial exceptions since 2026-09-16: still reached through the service-role client (`support_cases`, admin side) or otherwise closed off (`payment_reminders`, no UPDATE/DELETE), but each gained one real self-service policy — a planner/vendor can insert and read their own cases, and a planner can insert their own reminder-sent record. *(Phases 6–8.)*
- [x] **Unclaimed vendor stubs aren't visible to guests, contradicting the documented bootstrapping workflow.** `gather_erd_master.md` says unclaimed stubs should show in the public directory marked Unverified; the actual Phase 0 `vendors_select_public_or_own` RLS policy only lets `anon` see `verification_status = 'verified'` rows. Resolved in Phase 6: the policy now includes all three statuses, and the `/vendors` directory page itself was updated to actually query and badge all of them, not just verified ones. *(Phase 6.)*
- [x] **Background-job infrastructure for `payment_reminders` was open since Phase 0 — resolved 2026-09-16 by not needing it.** The stack table named two candidates (Supabase Edge Functions + `pg_cron`, or Inngest) but nothing ever picked one, because Andre redirected the ask when it came up: skip cron-based scheduling entirely and send reminders *implicitly* instead — triggered by a planner's own next visit to a page that would already show them a due/overdue payment, not by a timer. `payment_reminders` gained one real INSERT policy (self, owner-or-editor-collaborator only) instead of staying fully service-role-only. **A real, accepted limitation, not a gap to still close**: an installment due while nobody opens the app has no timeliness guarantee the way a true scheduled job would — genuine cron remains a plausible future upgrade if that turns out to matter in practice, not a mistake in this decision.
- [ ] **OKLCH brand palette has real, measured WCAG AA contrast failures.** Computed properly in Phase 9 (OKLCH → linear sRGB → WCAG relative luminance, not eyeballed): `primary` text on `bg` (3.87:1, needs 4.5), white button text on `primary` (4.36:1), `primary` text on `primary-soft` badges (3.28:1), and `success` green text on `bg` (2.07:1, a serious failure even for large text) all fail. None of this project's actual UI text is large enough (≥18.66px bold or ≥24px) to use the looser 3:1 threshold, so 4.5:1 is the real bar everywhere. **Deliberately not fixed**: these are the confirmed, stakeholder-approved brand colors from the ported prototype, and adjusting them is a design decision, not a technical one — flagging for a human call rather than unilaterally darkening the brand magenta. *(Found in Phase 9, needs a design decision.)*
- [ ] **The 2 new themes (Ocean Current, Sunset Social) haven't had the same numeric WCAG contrast check the Bold Playful palette got in Phase 9.** The design-system-v2 redesign (2026-09-14) added them with the same relative lightness/chroma shape as Bold Playful, on the reasoning that they'd fail or pass similarly — but that's an assumption, not a measurement. Same OKLCH → linear sRGB → WCAG relative-luminance script from Phase 9 should be re-run against both new palettes before treating this as closed. *(Opened 2026-09-14.)*
- [ ] **Persona-switcher bottom-sheet conversion — deliberately deferred, not forgotten.** The prototype's persona switcher is a bottom-sheet modal (dim scrim + sheet sliding up, drag-handle bar); the current implementation (`persona-switcher.tsx`) is an inline pill row. The design-system-v2 redesign (2026-09-14) added a `motion` `layoutId` shared-layout slide to the tab bar's active-tab indicator but explicitly did *not* convert the persona switcher to a bottom sheet — that's a different interaction pattern, not a re-skin, and needs its own explicit go-ahead rather than being silently bundled into a "make it feel more native" pass. *(Flagged 2026-09-14.)*
- [ ] **A harmless React console warning fires on any `notFound()` page.** Found while verifying Phase 10's `requireAdmin()` (which uses `notFound()` for non-admins), but it's not admin-specific — any existing `notFound()` call in the app (a bad event/vendor id, etc.) triggers it too. Traced to the root layout's flash-of-wrong-theme-prevention inline `<script>`. The obvious fix (`next/script` with `strategy="beforeInteractive"`) was tried and reverted — it silently broke the actual flash-prevention it was replacing, since that strategy only guarantees "before hydration," not "before first paint." Console-only, no user-visible effect; needs a real fix (or an accepted "won't fix," a maintainer's call) rather than another blind attempt. *(Found 2026-09-14.)*
- [ ] **Admin console JSX duplication.** A multi-agent code review (2026-09-14) found the "count header + list-or-empty-card" related-record section repeated ~14 times across the 3 admin detail pages (`planners/[id]`, `vendors/[id]`, `events/[id]`), plus a search-form + query-builder pattern repeated 3 times across the admin list pages, and badge-class lookup tables not shared between them. Real and worth doing, but a proportionate follow-up (likely a `RelatedList`/`AdminSearchForm` primitive in `src/components/ui/`), not folded into the same pass that fixed the security/correctness bugs the same review found. *(Found 2026-09-14.)*
- [ ] **Admin detail-page queries have no row caps, and `requireAdmin()` re-queries per request.** Same review pass: several admin detail-page sub-queries (attendees, quotes, event associations, etc.) have no `.limit()`, unlike the top-level list pages which are all capped at 100 — fine today, a real risk once any single event/vendor/planner accumulates enough related rows. Separately, `requireAdmin()` does a fresh `auth.getUser()` + `is_admin` RPC round-trip on every call, and both the admin layout and every admin page call it — wrapping it in React's `cache()` would dedupe that within a single request without weakening the "call it everywhere, no exceptions" rule Server Actions still need. *(Found 2026-09-14.)*
- [ ] **No real physical mobile device has ever tested this product.** Every phase's "mobile-first" verification has been Playwright/Chromium — real device presets (correct viewport, touch, pixel ratio) were added in Phase 9 and caught nothing new, but Chromium fundamentally cannot validate `env(safe-area-inset-*)` behavior the way WebKit/real iOS Safari does, a gap the architecture doc's own Verification section has named since Phase 2. This needs Andre's own phone, the same category of blocker as the still-pending Vercel login and Stripe account. *(Open since Phase 2; still open after Phase 9.)*

---

## Progress Log

Thin index only — the detailed dated account of what was actually built, verified, and fixed lives in `gather_web_architecture.md`'s "Local environment setup log". This section just points into it by phase, so there's one place recording "what actually happened" and no risk of the two docs drifting apart.

- **Phase 0 (Scaffolding)** — done 2026-09-12. Full detail, including the three real RLS bugs found and fixed, in `gather_web_architecture.md`.
- **Phase 1 (Design system)** — done 2026-09-12. Full detail in `gather_web_architecture.md`.
- **Phase 2 (App shell & cross-cutting state)** — done 2026-09-13, verified end to end with Playwright against a real dev server and local Supabase Auth user. Full detail, including the Server-Action/cookie-remount bug and the global-store-leak bug, in `gather_web_architecture.md`.
- **Phase 3 (Guest & public browse)** — done 2026-09-13, verified with real seeded data (a published event, a verified vendor) against a real dev server. Full detail, including the RSVP write's design and the vendor-visibility RLS gap found for Phase 6, in `gather_web_architecture.md`.
- **Phase 4 (Registration & onboarding)** — done 2026-09-13, verified end to end through the real UI for both planner and vendor paths (including the vendor self-registration → Owner path the Salesforce build never confirmed). Full detail, including the new `profiles.phone` migration and two Router-Cache/derived-state bugs, in `gather_web_architecture.md` and `teachAndre/04-derived-state-and-router-cache.md`.
- **Phase 5 (Planner core: events)** — done 2026-09-13, verified with two real signed-up users (owner + collaborator, separate browser contexts) through the full create/edit/invite/accept cycle. Full detail, including the ambiguous-PostgREST-embed bug, the new `events_select_pending_invitee` RLS policy, and the migration-tracking lesson, in `gather_web_architecture.md`.
- **Phase 6 (Vendor directory & claims)** — done 2026-09-13, verified with two real users through search → stub creation → dedupe-finds-the-stub → claim → pending-status-on-revisit. No new bugs found this phase. Full detail in `gather_web_architecture.md`.
- **Phase 7 (Commerce)** — done 2026-09-13, verified end to end (associate vendor → accept quote → create plan → installment-sum trigger correctly rejects an over-limit installment → mark paid → totals correct), both commerce invariants actually built (not just documented) for the first time. Full detail in `gather_web_architecture.md`.
- **Phase 8 (Vendor persona)** — done 2026-09-13, verified with the most thorough multi-actor test run so far: an invited teammate creates the exact booking they'll later manage *before* accepting the vendor invite, proving the live-join sharing fix and the persona switcher together, for real. Full detail, including the new `auth.users`-reading RLS helper, in `gather_web_architecture.md`.
- **Phase 9 (Polish & hardening)** — done 2026-09-13: notification preferences, a systemic accessibility fix reaching every form via the shared `Input`/`Button` primitives, real computed WCAG contrast numbers for the OKLCH palette (several confirmed failures, flagged for a design decision rather than silently fixed), and Vitest/RTL infrastructure (16 tests) scoped to what's actually testable given Vitest's own lack of async-Server-Component support. Two items honestly carried forward rather than closed: a real physical mobile-device test, and the background-job infrastructure decision for payment reminders. Full detail in `gather_web_architecture.md`.
- **Design system v2 (multi-theme + motion redesign)** — done 2026-09-14, applied across every existing route in one pass rather than staged. Two new themes (Ocean Current, Sunset Social), a `motion`-based spring/gesture system on `Button`/`Card`/`LinkCard`, staggered list entrances, a rebuilt marketing landing page, and theme-aware confetti on RSVP/quote-accept. Full detail, including the `motion.create()`-is-client-only build failure and the `acceptQuote` → `useActionState` refactor, in `gather_web_architecture.md`.
- **Mobile testing round 1 (4 UX fixes)** — done 2026-09-14, the first feedback from actually using the app on a phone. Owner/collaborator "Hosting"/"Invited" badges, a discoverable Payments nav entry, a new shared `Field` label primitive (closing 4 unlabeled/inconsistently-labeled inputs at once), and a manual RSVP editor for owners/editors. Also fixed `allowedDevOrigins` so the dev server is reachable from a phone at all. Full detail in `gather_web_architecture.md`.
- **Mobile testing round 2 (back navigation + payment-plan ownership clarity)** — done 2026-09-14. A new shared `BackButton` primitive applied across all 12 non-root drill-down pages, a missing "Add a vendor" link on the event-vendors empty state, and reworded Payments copy plus a new explicit Business Rule after confirming (not assuming) that payment plans were already correctly planner-only at the RLS layer — only the UI copy was ambiguous. Full detail in `gather_web_architecture.md`.
- **Vendor social links + vendor/event photo galleries (first use of Supabase Storage)** — done 2026-09-14. Two new Storage buckets (`vendor-gallery` public, `event-gallery` private + signed URLs), `vendor_social_links`/`vendor_gallery_images`/`event_gallery_images` tables, upload via Server Actions reading `FormData` directly. A real, non-obvious RLS bug (an inline `EXISTS` subquery silently failing inside a `storage.objects` policy specifically) found and fixed with a new `is_vendor_verified()` SECURITY DEFINER function. Full detail in `gather_web_architecture.md`.
- **Upcoming Tasks/Payments dashboard + expandable verification badge** — done 2026-09-14. A new `upcoming_window` preference (Profile), two new Home sections scoped to the user's own events, per-event pending-task/pending-payment badges on My Events, and the Verified/Unverified tag becoming a tap-to-expand icon (interactive only where it's safe — never nested inside a list row's own link). Also added a vendor test account (`TESTING.md`) since none existed before. Full detail in `gather_web_architecture.md`.
- **Full project code review** — done 2026-09-14, a multi-angle review across everything built to date. Found and fixed one real security vulnerability (`is_admin()` exposure, closed at both the RPC and Postgres column-grant level) and five real correctness/accessibility bugs (a quote-accept staleness bug that could silently revert an acceptance, a claim-approval flow that swallowed write errors, a silently-failing manual RSVP editor, an ARIA regression from combining `Field` with `Input`'s placeholder-derived label, and a `tailwind-merge` gap that broke a pill-shaped chip's styling) — every fix re-verified against real behavior, not just re-read. A further set of duplication/efficiency findings were logged as follow-up items above rather than fixed in the same pass. Full detail in `gather_web_architecture.md`.
- **Phase 10 (Admin & Support Console)** — done 2026-09-14, the full CRM-style console exactly as designed: `is_admin`/`requireAdmin()`/service-role access, the claim-approval queue (closing an item open since Phase 6), all 10 routes, and an audit log proven end to end (not just written) via a real Playwright pass. One implementation deviation from the written design (how `/admin`'s desktop shell was actually achieved) documented in `gather_web_admin_architecture.md`. Full detail in `gather_web_architecture.md`.
- **Gallery compression, 20-photo cap, and a real public-profile display treatment** — done 2026-09-14, a cost/UX follow-up to the previous gallery feature. Client-side Canvas-based image compression before upload (~96% size reduction measured on a real test photo), a shared `MAX_GALLERY_IMAGES` cap enforced identically on both galleries, and the public vendor profile actually using its gallery for the first time — a gradient "banner" treatment of the description when there are no photos, a full-bleed swipeable carousel (Andre's own pick among a few options) when there are. Two real bugs caught by this project's own build/console-check habits, not the diff: a `useActionState` transition-tracking warning from wrapping the dispatcher instead of building compression into the action itself, and a `"use server"` file silently losing all its exports when a plain constant was added to it. Full detail in `gather_web_architecture.md`.
- **Event Types, Service Categories, and Suggested Vendors** — done 2026-09-14, this session's largest single feature, run through full plan mode (two Explore passes, a Plan pass, an approved written plan) given the real architectural decisions involved. `events.event_type` (free text since Phase 5, never actually displayed) became a real admin-managed dropdown with an "Other" + free-text fallback; a second, separate admin-managed taxonomy (`service_categories`) tags vendor services; a many-to-many mapping between the two (this project's first true junction table) powers a new "Suggested Vendors" section on the event's own vendors page, matching vendors whose services fit the event's type. A real bug found by the first Playwright run: a conditionally-rendered form field (`eventTypeOther`, only present in the DOM when "Other" is picked) returns `null` from `formData.get()` when absent, not `undefined` — different from every other optional field in the same schema, which are always-present-but-empty (`""`). Full detail in `gather_web_architecture.md`.
- **Budget: line items, vendor confirmation, and a spend warning** — done 2026-09-14, built directly on the Event Types/Service Categories groundwork above. Four architectural forks (one vendor per budget line vs. comparing candidates; committed spend vs. budgeted vs. paid as the warning basis; an additive payment-plan tag vs. restructuring plan creation; a fixed vs. per-event warning threshold) were put to the user before planning, not assumed. New `budget_items` table (planner-only, same RLS shape as `event_tasks`), `events.budget_total`/`budget_warning_percent`, and `payment_plans.budget_item_id`. Also closed a real, pre-existing gap found during research: `event_vendors.confirmed` had existed since Phase 7 but nothing anywhere ever wrote to it — a planner can now actually mark a vendor Confirmed/Pending, reflected live on the new Budget page. Full detail in `gather_web_architecture.md`.
- **Chat vendor-verification gate + unread indicators** — done 2026-09-16. Closed a real gap Andre caught by asking: an unverified vendor's `event_vendor_messages` INSERT policy only checked team membership, not verification, so any freshly-created vendor stub could message a planner with zero admin review — fixed by gating the vendor-side branch on `is_vendor_verified` (planner side stays unrestricted). Separately added per-thread unread counts (a new `event_vendor_chat_reads` table, no Realtime/tab-bar badge — deliberately scoped down to a count next to each vendor row after Andre walked back an initial broader ask). Full detail in `gather_web_architecture.md`.
- **Vendor removal guarded against paid installments** — done 2026-09-16. A vendor/budget-item link can no longer be removed or unlinked while any of its installments is marked `paid` — a new `refunded` installment status and a shared `hasPaidInstallment()` check block `removeBudgetItem`, `unlinkVendorFromBudgetItem`, and a genuinely new `removeVendorFromEvent` action (soft-delete via `event_vendors.status`, since no DELETE policy exists and a real delete would cascade-wipe payment history). Full detail in `gather_web_architecture.md`.
- **Event countdown card** — done 2026-09-16, picked off a design-canvas mockup round (glass tiles on a gradient hero, reusing the `Button` component's own gradient tokens). A pure `getCountdownRemaining()` helper (tested) plus a client `CountdownCard` ticking every second, shown to guests and management view alike. Full detail, including the three-way `useSyncExternalStore`/`suppressHydrationWarning`/plain-interval decision tree, in `gather_web_architecture.md` and `teachAndre/12-persisted-ui-state-without-flicker.md`.
- **Proof-of-payment attachment** — done 2026-09-16. Each `payment_installments` row can now carry one attached receipt/screenshot (`proof_of_payment_path`), via a new private `payment-proofs` Storage bucket (images + PDF, editor-only insert/delete, both-sides select). Verified against the real local Supabase instance with two throwaway authenticated users, not just structurally. Full detail in `gather_web_architecture.md`.
- **UI polish pass — decluttered headers, Home gradient hero** — done 2026-09-16, off a design-canvas review of "cluttered headers" and "flat list pages." `BackButton` became a circular icon-only control and a new shared `PageHeader` component replaced the old stacked Back/title/badges/subtitle block on all 18 non-admin pages that had it; Home's dashboard gained a gradient hero with live quick-stats and icon-badge list rows, reusing the countdown card's and event-page nav grid's own visual language rather than inventing new ones. Full detail in `gather_web_architecture.md`.
- **Self-service case submission ("Report an Issue")** — done 2026-09-16, closing `gather_web_admin_architecture.md`'s own Open Question #2. New `/profile/cases`, `/profile/cases/new`, `/profile/cases/[id]` routes; `support_cases` gained a `category` enum (grounded in the app's real feature set) and an `attachment_path`, plus two new self-scoped RLS policies (insert/select own) — the table's first regular-user access pattern since Phase 10 locked it to service-role-only. A new `support-case-attachments` Storage bucket is keyed by the *uploading user's own id* rather than a case id, since the case row doesn't exist yet at upload time — see `teachAndre/10-supabase-storage-and-nested-rls.md`'s new section. Full detail in `gather_web_architecture.md`.
- **FAQ page** — done 2026-09-16. A public `/faq` page (`src/lib/faq-content.ts`), grouped by the same categories as the case-report form, using the existing `<details>`/`<summary>` accordion pattern rather than a new client component. Andre set a standing rule (now in `CLAUDE.md`) to keep this content re-checked against feature changes going forward, not just written once.
- **Database cleanup — 19 leftover test events removed** — done 2026-09-16. A backlog of dev-fixture events (fake tester personas, duplicate "Test Wedding"/"Confetti Test Party" runs) had accumulated across sessions and some were publicly visible on the live guest landing page; reviewed with Andre event-by-event for anything that looked like real content before deleting, including full cleanup of each event's Storage files and cascaded rows. Two events kept after explicit confirmation (`Sarah & Tom's Wedding`, `Phase8 Wedding`).
- **Documentation review pass** — done 2026-09-16, prompted by Andre asking for a general project review after several days of changes. Found and fixed real drift: this doc's own Progress Log and two stale "`support_cases` is service-role-only" claims, two missing changelog entries in `gather_web_architecture.md` (the FAQ page, the event cleanup above), a real FAQ content bug (a question implying read receipts that don't exist), and an untested pure function (`supportCaseCategoryLabel`). `gather_web_user_journeys.md` — written forward-looking against Phases 0–1 only and never updated since — was fully reconciled against shipped reality in a follow-up pass the same day, correcting two claims that were never actually true (a cited helper function that was never built; the vendor claim admin screen called "not yet built" when it's been live since Phase 10).
- **Email service wired in + implicit payment reminders** — done 2026-09-16. Andre asked to explore email/SMTP options; picked Resend (chosen for its first-class Next.js fit) for production, discovered and wired up the local Supabase CLI stack's own Mailpit email catcher (already running, just needed its SMTP port uncommented in `supabase/config.toml`) as the local-dev fallback when no `RESEND_API_KEY` is set — `src/lib/email.ts`'s `sendEmail()` picks between the two with no caller-side branching. Rather than build the long-deferred cron/scheduler (see Outstanding Items above), Andre redirected to an *implicit* trigger instead: `<ReminderChecker>` (mounted once in `AppShell` for every authenticated session) fires a one-shot server check on app open, which emails anything due-soon-or-overdue that hasn't already been reminded about and records it in `payment_reminders` (a new self-scoped INSERT policy) so it's never repeated. A real, unrelated bug got fixed along the way: `installment_status`'s `late` enum value has never once been written by any app code, so every "Overdue" label in the app (Home, My Events, the Payments page) was silently dead code — replaced with `isInstallmentOverdue()`, computed live from `due_date`, and a new small overdue indicator was added to the event page's own Payments tile (the one surface that had nothing at all before). Verified end to end with a real Playwright session against a throwaway user with a genuinely overdue fixture installment: confirmed a real email landed in Mailpit, confirmed exactly one `payment_reminders` row got created, confirmed all four UI surfaces (Home, My Events, event detail, Payments page) showed the overdue state correctly, and confirmed a second page load sent no duplicate email and created no duplicate row.
- **Vendor marketplace redesign — Featured placement, profile-completion ranking, a real logo** — done 2026-09-16, off a design canvas reviewed and adjusted with Andre before building (the Home teaser needed to be smaller, and needed a "See all" link into the real `/vendors` directory it was never meant to replace). Two ranking signals, both agreed with Andre before building rather than picked unilaterally: an admin-curated `vendors.is_featured` flag (a paid-placement seed) and a self-serve, never-stored profile-completion score (`getVendorCompletion()`, 5 real signals) that only the vendor's own dashboard ever sees as a percentage — planners only ever see the Featured/Verified badges. A new `vendors.logo_path` + public `vendor-logos` Storage bucket gives every vendor a real storefront photo, shown via a new shared `VendorAvatar` (falls back to a category-hashed gradient + initials, generalizing what the guest landing page already did ad hoc) everywhere a vendor is listed. `is_featured` needed its own hand-written migration, not just a Drizzle `ALTER TABLE` — a plain RLS row-policy can't restrict which columns an authorized UPDATE may touch, so this is the exact `profiles.is_admin` column-grant fix (`teachAndre/09`) applied a second time, closing a matching latent gap on `verification_status` while already in there. `/vendors` itself was rebuilt into an actual marketplace (a Featured row, a photo-forward grid), replacing the old flat text list and its manual Name A→Z sort — which the new ranking simply replaced outright rather than keeping both as parallel options. Verified end to end with Playwright against two throwaway vendor fixtures (Featured/100%-complete and plain/0%-complete): confirmed correct rendering on Home, `/vendors`, the guest landing page, and the public profile; uploaded a real logo through the actual edit-page file input and confirmed the dashboard checklist moved from 0% to 20%; confirmed, via a real signed-in (non-service-role) session, that a vendor owner's own attempt to set `is_featured` on their own vendor is rejected at the database, not just hidden in the UI.
- **Payment-plan/accepted-quote mismatch — surfaced, explained, and prevented going forward** — done 2026-09-16, caught by Andre's own real usage (`Phase8 Wedding`/`Phase8 Photography`, kept from the earlier test-event cleanup): an accepted R8,000 quote and a separately hand-typed R5,000 payment plan total for the same booking, showing as "over budget" with no explanation of where either number came from. `payment_plans.total_amount` had simply never been connected to `vendor_quotes.amount` at all. Fixed three ways: the vendor's own detail page gained an inline Payments summary (Total/Paid/Outstanding, matching how Quotes already renders there — Andre's own ask, "payments should also be listed like quotes, not just through Manage Payments"); a plain-language mismatch warning now shows on both the vendor page and the Budget page's own vendor row whenever a booking's accepted quote and its plan total disagree; and `CreatePlanForm`'s total field now prefills from the accepted quote (a default, not a lock — a planner can still type over it if the final price genuinely differs), which is the real fix, since the first two are diagnosis, not prevention. Verified with two real Playwright passes against throwaway fixtures: one recreating Andre's exact numbers confirmed both warnings render; a second, with an accepted quote and no plan yet, asserted the real DOM input actually arrives prefilled to the quote's value, not just that the prop was passed.
- **Task reminders** — done 2026-09-16, Andre's pick off a short "what's next" list as the feature the implicit-trigger reminder pattern already covered almost for free. A new `task_reminders` table (the `payment_reminders` twin) plus `checkAndSendTaskReminders()`, fired from the same `<ReminderChecker>` alongside the existing payment check: emails a planner about any incomplete `event_tasks` row due soon or overdue on an event they own or collaborate on (owner-or-any-collaborator, not editor-only — `event_tasks.assignedTo` was confirmed unused anywhere in the app first, so no assignment-aware routing was built around it), and records the attempt so it's never repeated. The owned-or-collaborator event lookup, about to be duplicated a second time, was pulled out into a shared `getMyEventIds()` used by both the payment and task checks. Profile's "Email payment reminders" checkbox relabeled to "Email reminders (tasks & payments)" since the same toggle now gates both. Verified end to end with a real Playwright session against a throwaway overdue task fixture: confirmed a real email landed in Mailpit, confirmed exactly one `task_reminders` row got created, and confirmed a second page load sent no duplicate email and created no duplicate row.
- **Vendor reviews & ratings + a launch-grid redesign of the vendor dashboard** — done 2026-09-16, planned with mockups first (Andre reviewed and picked the recommended option on all three open questions: contracted-bookings-only eligibility, star + written text, vendor can reply publicly), then a second mockup round for the dashboard itself once Andre flagged it as "getting long and cluttered." New `vendor_reviews` (one per booking+reviewer, contracted-only, public SELECT) and a deliberately separate `vendor_review_replies` table — not two more columns on the same row — since a shared-row UPDATE can't be column-restricted the way `vendors.is_featured` needed a real migration to fix, and unlike `vendor_quotes`' own both-sides-editable row, a review and its reply have adversarial incentives. New pages on both sides (`/vendors/[id]/reviews`, `/reviews/write`, `/vendor/[vendorId]/dashboard/reviews`). The vendor dashboard itself was rebuilt into the same "colorful tint grid" pattern the event page's own nav grid already established — 8 tiles (Bookings, a genuinely new read-only Payments overview, Services, Gallery, Social Links, the new Reviews, Team, Business Profile), each its own route instead of one long stacked page; the profile-completion checklist moved to the top of the Edit page, with just a condensed progress nudge left on the dashboard. Two real bugs caught by Playwright, not the diff: a `vendor_review_replies` embed came back as a single nullable object rather than an array (PostgREST infers cardinality from the reply table's own UNIQUE constraint), and the vendor's own collapsed reply view initially showed no indication of what they'd actually replied. Both fixed and re-verified. Full end-to-end Playwright pass against the real `phonetest@gather.dev`/`vendortest@gather.dev` accounts with a throwaway contracted booking: review submitted and rendered, edit-in-place confirmed, the dashboard grid's real badges (a Reviews count, a Bookings dot) confirmed against real data, the vendor's reply confirmed visible on both their own dashboard and the public profile.
- **Vendor directory cleanup + realistic seed data — and a more serious RLS bug the reviews feature above had actually shipped with** — done 2026-09-16. Removed 8 pure test/debug vendor stubs (duplicate dedupe-test fixtures, "Debug Vendor Co", timestamp-suffixed fixtures) out of 11 total, keeping and cleaning up the two tied to real content (`Bloom & Co Florals`, `Phase8 Photography`) plus the documented `Gather Test Vendor Co` test account. Seeded 7 realistic vendors across new categories (3 Featured), 6 throwaway reviewer accounts with real distinct names, and 27 reviews (real fake events/contracted bookings behind each one, ~40% with a vendor reply, ratings honestly varied rather than uniformly 5-star). That seeding pass immediately surfaced a real bug the same-day reviews feature had shipped with, invisible until then because every earlier test happened to run as either the reviewer or the vendor team (both of whom have legitimate visibility into their own booking): `getVendorRatingSummary`/`getVendorReviews` read through a PostgREST embed into `event_vendors`, which is deliberately *not* public — so reviews silently vanished for a genuine third-party planner just browsing, exactly the person the feature exists for. Fixed by denormalizing `vendor_id` directly onto `vendor_reviews` (both write policies re-verify it against the real booking, so it can't drift or be forged), which also closed a second, smaller gap found in the same migration — the review UPDATE policy had no `withCheck` at all, so a forged PATCH could previously have retargeted an existing review at a different booking with zero eligibility re-check. Re-verified with a real Playwright pass browsing as a genuine third party (`phonetest@gather.dev`, no relationship to any seeded booking): reviews and their reply now render correctly.
- **Ratings on vendor cards + a repositioned "Add to an event"** — done 2026-09-16, live feedback off the newly realistic marketplace. A new batched `getVendorRatingSummaries()` and shared `VendorRatingBadge` now show a real star average on every vendor card across `/vendors`, Home's teaser, and the guest landing page — one flat query per page, not one per card. Separately, "Add to an event" on the vendor profile page had quietly become the very last thing on the page once the same-day Reviews section landed above it — moved back to directly under the header, the first real action available rather than something requiring a full scroll to find.
- **My Events' empty state made actually tappable** — done 2026-09-16, caught by real phone usage (Andre's wife tapped it and nothing happened). "No events yet — create your first one." was a plain `Card`, not a `LinkCard` — visually identical to every real, tappable event row on the same page, but inert. Now a `LinkCard` pointing at `/events/new`, the same destination the header's own "+ New" button already uses. The same plain-`Card`-as-empty-state pattern repeats in roughly two dozen other places across the app (task lists, budget items, vendor services, etc.) — left alone for now since only this one was reported and several of those already sit right above their own inline "add" form, but worth an audit pass if it turns out to be a recurring source of confusion.
- **Empty-state audit + two more real fixes, plus a Profile section reorder** — done 2026-09-16. Audited all ~18 remaining "No X yet" empty states app-wide: most turned out fine as plain (non-tappable) `Card`s — either a filtered/search "no results" state with no single create action, a read-only list, or an add-form already rendered directly adjacent (making a link redundant). Two genuine gaps found and fixed the same way as My Events: `events/[id]/vendors`' empty state now links to `/vendors` (editor-only, matching the page's own existing "Browse all vendors" button's access gating), and `vendors/[id]/reviews`' empty state now links to the write-review flow, but only when the viewer actually has an eligible contracted booking (a new `getEligibleBookingForReview` check added to that page, which previously didn't compute eligibility at all). A third candidate (`admin/event-types/[id]`'s "add one first" message) was fixed the same way for correctness, though it isn't reachable in the current database — the service-categories taxonomy already has rows, so that branch never actually renders empty in practice. Separately, swapped the Profile page's Support and Theme section order per Andre's request. Two pre-existing lint errors (an unescaped quote, a `let` that should've been `const`) fixed in passing in files already being edited.
- **Profile identity hero** — done 2026-09-16, off a design canvas (two directions mocked, Andre picked the gradient hero). Replaced the flat "Signed in as Andre. / Edit Profile / Log out" card — a plain sentence plus two equal-weight controls fighting for attention — with a real identity block: an initials avatar (`vendorInitials()`, reused from the vendor-card work despite the name — the algorithm has nothing vendor-specific about it), the planner's name and email given real visual priority, in the exact same gradient-hero language Home's own "Welcome back" card already established rather than inventing a new visual style. Edit Profile is the one clear action (a solid white pill); Log out is demoted to a small circular icon button in the corner — the same treatment `BackButton` already uses — so it stops competing with Edit Profile for attention. The rest of the page (Support, Notifications, Theme) is untouched, per Andre's explicit ask to keep the Theme section as-is. Verified end to end with Playwright against the real `phonetest@gather.dev` account: the hero renders with real data, Edit Profile still navigates correctly, and the Log out icon still actually signs out and redirects home.
