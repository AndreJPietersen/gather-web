# Gather Web — Architecture & Migration Plan

_Started 2026-09-12. Replaces the Salesforce-era `gather_apex_architecture.md`/`gather_frontend_architecture.md` as the going-forward reference for this project. The Salesforce SFDX project (`eventio` repo) remains untouched as the design spec this build was ported from — see its `project/` docs for the original ERD, business rules, and LWC screen inventory this plan translates._

_Companion docs: `gather_web_user_journeys.md` (narrative Planner/Vendor flows told against the routes and RLS policies below), `gather_web_epic_roadmap.md` (the living, phase-by-phase execution plan — kickoff sequence, outstanding items, progress log index), and `gather_web_admin_architecture.md` (the internal-staff-facing admin/support console — a separate route namespace and authorization model layered on top of this same data, planned as Phase 10). This doc stays the stable reference: stack, data model, authorization, routes, and the dated engineering log of what's actually been built and verified._

## Context

Gather's backend was fully built on Salesforce (158 Apex classes, 253 passing tests, a working ERD) and the LWC frontend was partially planned/built on top of it. Investigating a licensing question (self-registering vendors need Account-create permission, which requires a Customer Community **Plus** license) surfaced a deeper structural problem: Salesforce Experience Cloud licenses are priced per external login/member — every additional free signup is a recurring cost that scales with the project's best-case outcome (growth). That's the wrong cost shape for a project explicitly optimizing for free signups and viral growth.

Decision made with the project owner: **fully migrate off Salesforce** to a modern, usage-priced web stack. The existing Salesforce data model and business rules (ERD, Apex architecture, the LWC screen/navigation inventory) were treated as a **design spec to port**, not code to reuse — this plan translates that spec into a new system, not a copy of the old one.

---

## Product Design Principles (carried forward)

Ported from `gather_erd_master.md`'s Design Principles — these shape *scope*, not just data model, so they belong up front rather than buried in a table:

- **Individual consumers are the primary target user** (weddings, birthdays, anniversaries). Professional/multi-client planners are a deliberately deferred, not-blocked use case — modeled later as an `event_collaborators` row with elevated `permission_level` on someone else's event, no new table required. **Implication:** don't build multi-client planner dashboards or portfolio views until this is explicitly prioritized.
- **Early on, most planners are self-managing their own event with little or no vendor-side participation** — the vendor directory starts empty/community-sourced (see Vendor Directory Bootstrapping below), and most vendors a planner deals with won't be actively using Gather themselves for a while. **Implication:** planner-facing tracking features (payment plans, tasks, attendees) must work fully standalone, without requiring the vendor on the other end to ever open the app — this is exactly why payment plans are planner-owned end to end (see the quotes-vs-payment-plans ownership rule in Business Rules & Invariants) rather than something negotiated with a vendor account.
- **Free directory model for now** — no commission/platform-fee fields anywhere in the schema. Monetization mechanics are undecided and out of scope. **Implication:** resist adding payout/commission columns to `vendors`/`event_vendors` speculatively.
- **Manual payment tracking today, gateway integration planned.** `payment_installments` already reserves `payment_gateway`/`gateway_transaction_id`/`gateway_status` columns for Stripe (Phase 7), but nothing calls a gateway yet — every installment is marked paid by hand. **Implication:** don't wire Stripe early; the schema is already shaped so that phase is additive, not a migration.
- **South Africa-only for launch: single currency (ZAR), fixed timezone (SAST/Africa Johannesburg)** — an explicit placeholder, not permanent architecture. Every amount is a plain ZAR number (no currency-code column), and `events` stores real `timestamptz` values (not a hardcoded offset) specifically so a future multi-timezone picker is additive. **Implication:** hardcode `Intl.NumberFormat('en-ZA', { style: 'currency', currency: 'ZAR' })` for now rather than building currency selection; see Open Questions below for what changes later.
- **Mobile-first** — planning happens primarily from a phone. Already enforced procedurally, not just as a principle: see this doc's Verification section for the real-device testing requirement (`npm run dev:mobile`, phone testing over every Vercel preview) before any phase is considered done.

---

## Recommended Stack (reasoned out)

| Layer | Choice | Why |
|---|---|---|
| Frontend | **Next.js (App Router, React)** | Largest ecosystem, easiest to hire for later, first-class support on every host below. Server Components + Server Actions replace a lot of what Apex controllers did. |
| Database | **Postgres via Supabase** | Supabase = hosted Postgres + Auth + Storage + **Row Level Security** in one free-tier product. RLS is a near-exact conceptual replacement for Salesforce's OWD-Private-plus-explicit-sharing model the ERD already relies on — same mental model, different syntax. Not a lock-in: it's plain Postgres, portable to any Postgres host if you ever outgrow Supabase. |
| Auth | **Supabase Auth** | Bundled, free up to a generous MAU threshold, integrates directly with RLS via `auth.uid()`. Handles email/password + social login, replacing `Site.createExternalUser`/`SalesforceSiteRegistrar` entirely — and removes the whole license-tier problem that started this investigation, because there's no per-login fee at all. |
| ORM | **Drizzle** | Thin, SQL-close, fully typed — a good match for a developer used to writing precise Apex/SOQL rather than fighting a heavy abstraction. |
| Background jobs | **Supabase Edge Functions + `pg_cron`**, or **Inngest** if durable retries/step functions are needed | Replaces the hourly reminder Queueable. Inngest's free tier gives Salesforce-Queueable-like per-step retry semantics if the reminder/payment logic gets complex. |
| Payments | **Stripe** | The original ERD already reserved `Payment_Gateway__c`/`Gateway_Transaction_Id__c`/`Gateway_Status__c` fields for this — carries a design decision already made, not a new one. |
| File storage | **Supabase Storage** | Cover images, quote documents — same product as the DB/auth, one less vendor to integrate. |
| Hosting | **Vercel** (app) + **Supabase** (data/auth/storage) | Both have real free tiers today and both bill on usage (bandwidth/compute/rows), not per registered user — the actual property being optimized for. Flag: Vercel bandwidth pricing is the one line item worth watching if traffic gets very large; Supabase/Postgres compute is cheap and predictable by comparison. |

This is "boring technology" on purpose — every piece here is mainstream, well-documented, and replaceable independently, so a big traffic spike is a scaling problem to solve with money, not a rewrite.

---

## Data Model Port (Salesforce ERD → Postgres)

| Salesforce entity | Postgres table | Notes |
|---|---|---|
| Person Account / User | `users` (managed by Supabase Auth) + `profiles` | Profile row keyed to `auth.users.id`. |
| Event__c | `events` | `owner_id`, `status`, `visibility`, `event_type`, `start_at`/`end_at` (real `timestamptz`, no hardcoded timezone), `capacity`, `location`, `description`, `cover_image_url`. |
| Event_Collaborator__c | `event_collaborators` | Join table: `event_id`, `user_id`, `permission_level`, `status`. Drives an RLS policy, see below. |
| Event_Attendee__c | `event_attendees` | `event_id`, nullable `contact_user_id`, fallback `name`/`email`/`phone`, `rsvp_status`, `guest_count`. |
| Event_Task__c | `event_tasks` | `event_id`, `title`, `due_date`, `completed`, `priority`, `assigned_to`. |
| Event_Vendor__c | `event_vendors` | `event_id`, `vendor_id`, `status`, `amount`, `confirmed`. |
| Account (Vendor record type) | `vendors` | Discriminator not needed as a separate record type — vendors get their own table instead of sharing `accounts`. `verification_status`, `primary_category`, `created_by`. |
| Vendor_Service__c | `vendor_services` | `vendor_id`, `name`, `description`. |
| Vendor_Claim_Request__c | `vendor_claim_requests` | `vendor_id`, `status`, `notes`, `reviewed_by`, `reviewed_at`, `rejection_reason`. Partial unique index enforces one Approved per vendor. |
| Vendor_Team_Invite__c | `vendor_team_invites` | `vendor_id`, `invited_email`, `role`, `status`. |
| AccountContactRelation | `vendor_team_members` | `vendor_id`, `user_id`, `role` (owner/manager/staff), `is_active`. |
| Vendor_Quote__c | `vendor_quotes` | `event_vendor_id`, `amount`, `status`, `valid_until`, `document_url`. Partial unique index: one Accepted per `event_vendor_id`. |
| Payment_Plan__c | `payment_plans` | `event_vendor_id`, `total_amount`, `deposit_amount`, `deposit_due_date`, `status`. |
| Payment_Installment__c | `payment_installments` | `payment_plan_id`, `installment_number`, `due_date`, `amount`, `status`, `paid_on`, `payment_gateway`, `gateway_transaction_id`. CHECK/trigger keeps sum ≤ plan total. |
| Payment_Reminder__c | `payment_reminders` | `payment_installment_id`, `remind_at`, `method`, `sent`. |

**Invariant enforcement** (was Apex trigger "backstops"): use **partial unique indexes** and **CHECK constraints** at the DB level in addition to app-level checks — e.g. `CREATE UNIQUE INDEX ON vendor_quotes (event_vendor_id) WHERE status = 'Accepted'`. This is strictly better than the Salesforce version: it's synchronous and unconditional, no async recalculation lag to work around.

---

## Business Rules & Invariants

Consolidated from `gather_erd_master.md` (scattered across Design Principles, object notes, and the Persona Model section) into one reference, each tagged with where it's actually enforced:

| Rule | Enforced where |
|---|---|
| Only one `Accepted` quote per `event_vendor_id` at a time | Partial unique index on `vendor_quotes` (see Data Model Port above) |
| Only one `Approved` claim per vendor | Partial unique index on `vendor_claim_requests` |
| **Approving a claim auto-rejects every other competing `Pending` claim on the same vendor** (multiple planners/vendors can submit competing claims; only one can win) | Server Action (`approveClaim`) — reject the others in the same transaction as the approval, not just relying on the unique index to block a second approval later |
| Installment amounts for a plan can never sum to more than the plan's `total_amount` | CHECK constraint / trigger on `payment_installments` |
| Vendor role tiers — **Owner** (edit business profile, manage team membership) > **Manager** (submit/manage quotes, manage `vendor_services` listings) > **Staff** (view-only on bookings/quotes) | `assertVendorAccess(vendorId, minRole)` Server Action guard (see Authorization Model Port below), re-checked per call |
| A vendor self-registering a brand-new business becomes its Owner immediately, no review needed (no competing claim possible on a listing that didn't exist yet) | Application logic in the vendor-registration Server Action |
| A claimant becomes Owner the moment their claim is approved | Application logic in `approveClaim`, same transaction as the auto-reject rule above |
| Before creating a new vendor stub, the UI must require searching existing vendors first and confirming no match | UX-level dedupe step in the add-vendor flow — not a DB constraint, matching the Salesforce-era decision that a hard uniqueness constraint on vendor name would produce false positives |
| A professional/multi-client planner is a deferred, not-blocked persona — modeled as an `event_collaborators` row with elevated permission, no new table | Schema already supports it (see Product Design Principles above); the actual elevated-permission UI is unbuilt — tracked in Open Questions below |
| **Quotes and payment plans have deliberately opposite ownership** — a quote is created and revised by the vendor (owner/manager) and only accepted/declined by the planner; a payment plan is created and maintained entirely by the planner (owner/editor collaborator) — the vendor can see it (so they know what's been agreed) but never write to it. This isn't a UI convention, it's enforced at the RLS layer: `vendor_quotes_insert_vendor_manager` vs. `payment_plans_insert_event_editor`/`payment_plans_update_event_editor`, which has no vendor-side branch at all (see the schema's own comment on that policy). Worth stating explicitly here because the two features sit right next to each other in the UI and are easy to mentally merge into "the vendor manages the money side" — they don't; a payment plan is closer to a planner's private budget tracker that happens to be scoped to a specific vendor booking, not a shared/negotiated document the vendor edits. |

## Vendor Directory Bootstrapping & Verification

Ported from `gather_erd_master.md`'s "Vendor Directory Bootstrapping & Verification" section — at launch the directory has no seed data and no vendor has self-registered, so it's **community-sourced**:

1. **Creating a stub.** Any authenticated planner can create a `vendors` row with just a name, category, and basic contact info (phone/website) — from `/vendors/add` (see Screen/Route Inventory below), after the dedupe-search step above confirms no match exists. `verification_status` starts at `unclaimed`.
2. **Directory visibility.** Unclaimed vendors appear in the public `/vendors` directory like any other, marked with an **Unverified** badge — this is what makes the crowd-sourced approach useful before real vendors arrive, and gives a vendor a reason to notice and claim their listing.
3. **Claiming.** A vendor finds their (possibly stub) listing at `/vendors/[id]/claim`, submits a `vendor_claim_requests` row with a short verification message. `verification_status` moves to `claim_pending`.
4. **Review.** Unlike Salesforce (an admin list view + Flow inside Setup — no custom screen needed), gather-web has no back-office by default, so this needs an **actual admin screen** — tracked in "What must actually get fixed this time" below as net-new work, not a straight port. Approving provisions the claimant as Owner in `vendor_team_members`, flips `verification_status` to `verified`, and auto-rejects any other competing `Pending` claims (see Business Rules & Invariants above). An admin can also mark a vendor Verified directly, without a claim, for hand-onboarded vendors.
5. **Duplicate vendors** that slip through the UX dedupe step are an admin merge, not a blocking concern — same as the Salesforce-era decision.

---

## Authorization Model Port (Sharing → RLS)

- **Owner-only until shared** (`events`, `vendors`): default-deny RLS policy, `USING (owner_id = auth.uid())`, then an additional `OR EXISTS (SELECT 1 FROM event_collaborators WHERE event_id = events.id AND user_id = auth.uid())` clause — a direct translation of `EventSharingManager`'s explicit-grant-on-invite-accept pattern.
- **Vendor team visibility** (`event_vendors` visible to the vendor's whole active team): RLS policy joining through `vendor_team_members` — same shape as `EventVendorSharingManager`, but computed live on every query instead of snapshotted at association time. This is a real improvement over the documented Salesforce limitation ("a teammate added after a booking already existed won't see it") — carry over the fix, not the bug.
- **Guest/public access** (published events, vendor directory): a plain RLS policy for `anon` role, `USING (status = 'Published' AND visibility = 'Public')` — no async Guest User Sharing Rule recalculation to reason about at all, a direct simplification.
- **Guest writes** (RSVP): keep the "narrow allow-listed DTO, not raw table access" principle from `GuestRsvpRequest` — implement as a Server Action or API route with an explicit small input shape, using the Supabase service role internally, never a client-side anon INSERT policy on the raw table.
- **Role-tiered vendor permissions** (Owner > Manager > Staff): a small `assertVendorAccess(vendorId, minRole)` helper (direct port of `UserContextService.assertVendorAccess`) called at the top of every vendor-scoped Server Action, re-checked per call — same stateless-per-call philosophy as the Apex version, not cached in a session.

---

## Screen/Route Inventory (LWC epics → Next.js routes)

| Area | Routes | Carries over from |
|---|---|---|
| Guest/shared | `/`, `/vendors`, `/vendors/[id]`, `/events/[id]` | `gatherHome`, `gatherVendorDirectory`, `gatherVendorProfile`, `gatherEventDetail` — **this time, `/` must branch on auth state** (never done in the LWC build). |
| Onboarding | `/register`, `/onboarding/planner`, `/onboarding/vendor` | `gatherRegisterCredentials`, `gatherPlannerProfileSetup`, `gatherVendorRegistration` — Supabase Auth removes the license blocker entirely. |
| Planner — events | `/events`, `/events/new`, `/events/[id]/edit`, `/events/[id]/attendees`, `/events/[id]/tasks` | `gatherMyEvents`, `gatherEventWizard`, sub-sections of Event Detail. |
| Planner — vendors/commerce | `/events/[id]/vendors`, `/events/[id]/payments`, `/vendors/add`, `/vendors/[id]/claim` | `gatherAddVendor`, `gatherVendorClaim`, `gatherEventVendors`, `gatherEventPayments`. |
| Vendor persona | `/vendor/[vendorId]/dashboard`, `/vendor/[vendorId]/team` | `gatherVendorDashboard`, `gatherVendorTeam`. |
| Shared | `/profile`, persona switcher in the top-level layout | `gatherProfile`, `gatherPersonaSwitcher` — becomes React Context/Zustand store instead of LMS (fixes the documented "late subscriber never gets initial publish" bug by using reactive state instead of one-shot events). |

Design system: Tailwind theme file carrying the confirmed OKLCH tokens (`--primary` magenta, `--secondary` golden-yellow, pill-shaped 999px radii, 2px borders, Fredoka + Nunito via `next/font`) straight over from `gather-bold-playful.html` — no CSP/font-hosting problem exists in this stack, so that whole Salesforce-specific workaround disappears.

---

## What becomes moot (Salesforce-specific, does not carry over)

Account-creation license restriction; `Gather_Member_Access` permission-set-assignment gap; Person Account self-edit sharing errors; `RunSpecifiedTests` coverage fragility; LWR CSP font-loading workaround; LMS late-subscriber replay bug; `DigitalExperience`/`ExperienceBundle` metadata retrieval quirks. None of these exist once there's no Salesforce sharing/licensing/permission model underneath.

## What must actually get fixed this time (universal, carries forward as real product work)

Guest home not branching on auth state; RSVP validation robustness; URL not reflecting current tab/view (a router now makes this free, not extra work); bare `R{amount}` string formatting → real `Intl.NumberFormat` currency formatting; vendor-team "snapshot at association time" sharing semantics (fix via live RLS join, above); claim approve/reject needs a minimal back-office UI (was deliberately out of scope before — now scoped as Phase 10, see `gather_web_admin_architecture.md`); notification preferences need an actual schema (never existed); and — called out explicitly because it never happened once across 22 LWC bundles — **this rebuild should have real component/integration tests from the start** (Vitest + React Testing Library), not deferred to "later."

---

## Open Questions (carried forward)

Ported from `gather_erd_master.md`'s Open Questions — these don't block current work but need answers eventually:

1. **Professional planner permission mechanics.** `event_collaborators` is designed to support this later (see Product Design Principles above), but the exact permission model — can a collaborator manage payments? invite other collaborators? — isn't defined yet.
2. **Multi-currency / multi-timezone**, once the target market broadens beyond South Africa. Both are deliberately deferred as one future migration rather than two, since `events` already stores real `timestamptz` values and adding a currency-code column alongside the existing plain-ZAR amounts is additive either way.
3. **`event_type` as a Postgres enum vs. an admin-configurable table.** Currently a plain `text` column (not even a `pgEnum`) — the loosest possible placeholder — deliberately left unconstrained rather than guessing at a fixed value set upfront; revisit once real usage shows whether a `pgEnum` (fixed set, needs a migration to extend) or an admin-configurable table (editable without a deploy) fits better.

The Salesforce-era Experience Cloud licensing question (Partner vs. Customer Community Plus for self-registering vendors) is **moot** here — see "What becomes moot" above; Supabase Auth has no per-login license tiers, and the equivalent self-registration flow has already been proven end-to-end in the Phase 0 RLS verification (see the engineering log below).

---

## Build Phases

See `gather_web_epic_roadmap.md` for the full phase-by-phase breakdown, the Phase 2 kickoff sequence, and the running Outstanding Items checklist. Summary: Phases 0 (scaffolding) and 1 (design system) are done and verified below; Phase 2 (app shell & cross-cutting state) is next.

---

## Verification

Each phase: deploy to a Vercel preview branch against a Supabase branch/dev database, exercise manually in a real browser (not just curl — a real gap in the old build), and add Vitest/RTL tests (`npm test`) for the components and Server Actions built in that phase before moving on. Data-model invariants (one Accepted quote, one Approved claim, installment sum ≤ plan total) get a direct SQL test against the constraints, not just app-level assertions.

**A real limit on what "add tests" can mean here**: per Next.js's own Vitest guide, async Server Components aren't supported by Vitest at all — and every `page.tsx` in this project is one. Vitest/RTL's job is the shared UI primitives (`src/components/ui/*`) and pure functions (`src/lib/utils.ts`); pages and Server Action business logic stay covered by the manual real-browser pass each phase already does, which is the correct strategy for that layer, not a stopgap for it.

**Mobile-first real-device testing** (this is a bottom-tab-bar, mobile-first product — desktop devtools emulation is not sufficient on its own): during active development, run `npm run dev:mobile` (binds to `0.0.0.0`) and hit `http://<pc-local-ip>:3000` from a phone on the same Wi-Fi for live-reload testing; when that's blocked (cellular data, network client isolation, or a flow needing real HTTPS such as an auth redirect) use a tunnel (Cloudflare Tunnel / ngrok) for a real `https://` URL to the local server. Once deploying regularly, each Vercel preview URL doubles as the real-device test target and should be opened on an actual phone, not just resized in a desktop browser, before a phase is considered verified — specifically checking iOS Safari's `100vh`/viewport-height quirks and safe-area insets around the fixed bottom tab bar, which devtools emulation does not reliably surface.

---

## Local environment setup log

- **2026-09-12**: Repo scaffolded at `C:\Dev\Eventio\gather-web` via `create-next-app` (TypeScript, Tailwind, App Router, ESLint). Installed `drizzle-orm`, `drizzle-kit`, `postgres`, `@supabase/supabase-js`, `@supabase/ssr`, and the `supabase` CLI as project dependencies. Vercel CLI installed globally. Chose the **full local Docker + Supabase CLI stack** for local Postgres/Auth (over a cloud-only dev project) — this requires Docker Desktop, which in turn requires WSL2, both needing Administrator privileges to install. Docker Desktop additionally needs CPU virtualization enabled in the BIOS/UEFI firmware (a separate setting from WSL2 itself — Windows reports it via `systeminfo`'s "Virtualization Enabled In Firmware" line). See root `README.md` for the one-time setup commands.

- **2026-09-12 (later same day): full Phase 0 schema + RLS baseline built and verified against the real local stack**, not just type-checked. What shipped:
  - All 15 tables from the Data Model Port table above, in `src/db/schema.ts`, using Drizzle's `pgEnum`/`pgTable`/`index`/`pgPolicy` and the `drizzle-orm/supabase` helpers (`authUid`, `authenticatedRole`, `anonRole`). Every table has `.enableRLS()` — deny-by-default, matching Salesforce's Private OWD posture — with real policies on `events`, `event_collaborators`, `vendors`, `vendor_team_members`, and `event_vendors` (the Phase 0 "baseline" scope); every other table is locked to service-role-only until its owning feature phase adds policies.
  - Indexes on every foreign-key column an RLS policy actually filters/joins on (`events.owner_id`, both `event_collaborators` FKs, `vendors.created_by`, both `vendor_team_members` FKs, both `event_vendors` FKs) — Postgres doesn't auto-index FK columns, and RLS policies run `EXISTS` subqueries on exactly these columns on every request.
  - A Postgres trigger (`supabase/migrations/00000000000001_profiles_on_signup.sql`) that creates a `public.profiles` row automatically whenever `auth.users` gets a new row via Supabase Auth signup, plus a `profiles.id → auth.users.id ON DELETE CASCADE` foreign key Drizzle can't express (it doesn't manage the `auth` schema).
  - Four `SECURITY DEFINER` SQL helper functions (`supabase/migrations/00000000000002_rls_helper_functions.sql`: `is_event_owner`, `is_event_collaborator`, `is_vendor_creator`, `is_vendor_team_member`) that every cross-table policy now calls instead of inlining `EXISTS` subqueries against another RLS-protected table.
  - The Next.js side of Supabase Auth: `src/lib/supabase/{client,server,middleware}.ts` (browser client, Server Component/Action client, session-refresh helper) and `src/proxy.ts` — note **`proxy.ts`, not `middleware.ts`**: Next.js 16 renamed the middleware file convention to "Proxy" (same mechanism, same `matcher` config, exported function renamed from `middleware` to `proxy`); starting a project on 16.3.5 should use the new name from day one rather than build on something already deprecated.
  - `npm run build` passes cleanly (no deprecation warnings, no type errors) and a real signup → trigger → profile row chain was proven against the running local Auth API, not just asserted.

  **Three real bugs found and fixed by testing through the actual enforcement path, not just structurally inspecting the schema — this is the reason none of this was declared "done" after the first `drizzle-kit push` succeeded without error:**
  1. **`drizzle-kit push` silently drops RLS policy conditions.** After pushing the full schema, `pg_policies.qual`/`with_check` were `NULL` for every single policy — `push` diffs live against the DB via introspection rather than executing the generated SQL file verbatim, and has a real bug serializing policy expressions through that path (confirmed by comparing against `drizzle-kit generate`'s output, which had the correct SQL all along). **Fix: never use `db:push` once RLS policies exist — use `db:generate` then `db:migrate` instead**, which applies the migration file's SQL directly. `db:push` is now a hard-failing script in `package.json` with a pointer back here, specifically so this can't be silently reintroduced later. The very first migration had to be applied by hand (`node -e` running the raw SQL, since `drizzle-kit migrate` didn't yet have a tracking-table row for it) and then retroactively marked as applied in `drizzle.__drizzle_migrations` (hash = sha256 of the migration file's exact bytes, `created_at` = the journal's `when` value) so `db:migrate` would treat it as a baseline going forward.
  2. **"infinite recursion detected in policy" (Postgres error 42P17).** `events`' SELECT policy queried `event_collaborators` to check for an accepted invite; `event_collaborators`' own SELECT policy queried `events` back to check for ownership — two tables whose RLS policies reference each other form a cycle, since evaluating either table's policy requires re-evaluating the other's. The same cycle existed independently between `vendors` and `vendor_team_members`. **Fix: route every cross-table (and self-referencing) RLS check through a `SECURITY DEFINER` SQL function** (the four functions listed above) — such a function's internal query runs as the function's owner, which bypasses RLS, breaking the cycle. This is a general Postgres/Supabase RLS rule, not something specific to a mistake in this schema: any time two tables' policies reference each other, or a table's policy references itself, this same fix applies.
  3. **`DROP SCHEMA public CASCADE` (used once, mid-debugging, to get a clean slate) wipes Supabase's own baseline grants on that schema**, not just the tables in it — `anon`/`authenticated`/`service_role` lost table-level SELECT/INSERT/UPDATE/DELETE entirely, producing a confusing "permission denied for table events" *after* the RLS fixes above were already correct (RLS only ever restricts further; it can't grant access a role doesn't already have at the privilege level). Restored via the standard Supabase grants, saved in `supabase/migrations/00000000000003_restore_public_grants.sql` for the record. **Lesson: never `DROP SCHEMA public` on a Supabase-managed Postgres instance — use `supabase db reset` for a genuine clean slate**, which reruns Supabase's own initialization (grants included) plus everything under `supabase/migrations`.

  **How this was actually verified** (via the real REST API with real user JWTs, not the Postgres superuser connection used for setup — a superuser bypasses RLS entirely, so querying through it proves nothing about enforcement): created two real users via the local Auth API, had User A create a draft/private event, confirmed User B and an anonymous request both got `[]` back, confirmed User A could read their own event, confirmed User B's attempted `PATCH` affected zero rows, published the event as User A, confirmed the anonymous request could now see it. Separately proved the `vendors`/`vendor_team_members` bootstrap case: User A self-registers a vendor and inserts themselves as its first Owner team member (the exact chicken-and-egg shape that broke the Salesforce build), confirmed User B cannot insert themselves as Owner on A's vendor, confirmed User A (now Owner) can add User B as Staff.

- **2026-09-12 (Phase 1: Design System Foundation) — done and verified in a real browser, not just built.** `src/app/globals.css` now carries the full OKLCH token set ported verbatim from `project/prototypes/gather-bold-playful.html` (the confirmed design direction) via a Tailwind v4 `@theme inline` block; `src/app/layout.tsx` loads Fredoka (display) and Nunito (body) through `next/font/google` — which self-hosts them automatically at build time, so the prototype's Google Fonts `@import`/CSP problem from the original Salesforce-era plan simply doesn't exist in this stack, nothing to port. Added `src/components/ui/{button,input,card}.tsx` as the first base primitives (pill-shaped buttons in primary/secondary variants, a bordered field-radius input, a card), plus `src/lib/utils.ts`'s `cn()` helper (`clsx` + `tailwind-merge`). Verified with Playwright against the real running dev server (not just a clean build): screenshotted `src/app/page.tsx`'s style-proof content, confirmed via computed styles that the `<h1>` actually resolves to `Fredoka, "Fredoka Fallback"` and the primary button/background actually resolve to the intended OKLCH colors (not browser defaults), and confirmed zero console errors. Screenshot matched the prototype's visual language (pill buttons, soft badge pills, rounded cards) directly.

- **Still open for Phase 0**: a Vercel deploy of the empty shell. `vercel whoami` shows the CLI is logged out — logging in (`vercel login`) is an interactive, browser-based step tied to a personal Vercel account, so it needs to be run by Andre directly rather than by Claude. Once logged in, the remaining steps (`vercel link`, then a deploy) are quick.

- **2026-09-13 (Phase 2: App Shell & Cross-Cutting State) — done and verified against a real running dev server + local Supabase Auth, not just built.** What shipped: `src/lib/session.ts` (`getSessionContext()` — the direct replacement for the Salesforce build's `SessionContextController.getSessionContext`, reading Supabase Auth plus every active vendor-team persona fresh on every call, never cached, specifically to avoid the documented LMS "late subscriber never gets the initial publish" bug); a per-request persona store (`src/lib/stores/persona-store.ts` + `src/components/providers/persona-provider.tsx`, Zustand `vanilla` store handed down through React Context rather than a module-level singleton — see below for why); a global toast store + renderer (`src/lib/stores/toast-store.ts`, `src/components/toast/toast-container.tsx`); the bottom tab bar (`src/components/nav/tab-bar.tsx`, guest vs. Planner vs. Vendor tab sets) and persona-switcher pill row (`src/components/nav/persona-switcher.tsx`, shown only when a user has more than one persona); `src/components/app-shell.tsx` tying it together, wired into `src/app/layout.tsx` (now `async`, calling `getSessionContext()` server-side on every request, plus a `viewport` export with `viewportFit: "cover"` so `env(safe-area-inset-bottom)` actually resolves for the fixed tab bar). Also shipped, as necessary infrastructure to verify any of this in a real browser rather than just structurally: a minimal `/login` page (Server Action `signIn`, `useActionState` for error display) and a `/profile` stub with a working sign-out, plus placeholder pages at `/vendors`, `/events`, and `/vendor/[vendorId]/dashboard` so the tab bar has somewhere real to land before their owning phases build real content.

  **Two real things found and fixed, not just assumed correct from the plan:**
  1. **A cookie-mutating Server Action discards its own `useActionState` return value before the client can react to it.** The original login form tried to detect a `{ success: true }` return value client-side (to show a toast, then redirect) via a `useEffect`. It never fired — `state` stayed `{}` forever — even though sign-in genuinely succeeded (the tab bar had already flipped to the authenticated set). Cause: Next.js automatically re-renders "the current page" in the same response whenever a Server Action mutates a cookie (ours sets the Supabase auth cookie), and that re-render is closer to a fresh remount than a state update — it discards the in-flight action state before any client-side effect watching it can run. **Fix: redirect server-side, inside the action, on the success path** (`redirect("/")` from `next/navigation`) instead of returning a value for the client to act on; the error path never mutates a cookie, so `useActionState` works normally there. See `teachAndre/03-server-actions-and-client-state.md` for the full writeup — this is a real Next.js behavior worth knowing before it bites the next form that signs someone in or out.
  2. **A plain global Zustand store would have leaked one user's session data into another's.** The persona-switcher needs to start pre-loaded with the signed-in user's actual personas, computed server-side per request — but a module-level `create(...)` store is a per-*process* singleton, not a per-*request* one, and Next.js reuses the same server process across many users' requests. Seeding a global store with server-fetched, per-user data at creation time risks that data leaking into a different user's initial render. **Fix: create a fresh store per component instance** (`createPersonaStore()` called inside `PersonaProvider` via `useState`'s lazy initializer, handed down through React Context) instead of a module-level export — the toast store, by contrast, stays a plain global singleton safely, since it only ever starts empty and is filled in client-side after hydration, never seeded with server data.

  **How this was actually verified**: a real `npm run dev` server against the real local Supabase stack, driven with Playwright (a real signed-up-via-admin-API test user, not a mock) through the full guest → login → authenticated-tab-set → profile → logout → guest-tab-set-again cycle, checking the browser console for errors at every step (none found) and screenshotting each state. `npm run build` and `npm run lint` both pass clean.

- **2026-09-13 (Phase 3: Guest & Public Browse) — done and verified with real seeded data against the real local stack.** What shipped: the real guest Home (`src/app/page.tsx`, replacing Phase 1's style-proof content) branching on auth state — a guest sees the hero CTAs, an authenticated user sees a personalized welcome and quick links — with both variants sharing the same live public-events and verified-vendor-teaser queries below; the real vendor directory (`src/app/vendors/page.tsx`, category pills + keyword search via plain GET search params, no client JS needed for search itself) and vendor detail page (`src/app/vendors/[id]/page.tsx`); the guest event detail + RSVP page (`src/app/events/[id]/page.tsx`, `rsvp-form.tsx`, `actions.ts`) — Phase 3 scope only (public + published events; the owner/collaborator management branch is Phase 5's job, added to this same route rather than rebuilt). Added `src/lib/supabase/service.ts` (the service-role client, used only for the RSVP insert) and `zod` (new dependency) for real server-side form validation — replacing the Salesforce build's flagged-as-a-gap bare `if (!name || !email)` check with actual field-level rules (name length, email format, guest-count bounds), enforced both by the browser's native `type="email"` validation and, independently, server-side in the Server Action, which is what a crafted request without JS would still hit. Also added `LinkButton` to `src/components/ui/button.tsx` (a `<Button>`-styled `<Link>`, since nesting an actual `<button>` inside an `<a>` — the first instinct — is invalid HTML) and `formatEventDateTime()` to `src/lib/utils.ts` (hardcoded to `Africa/Johannesburg`/`en-ZA`, per the ZAR/SAST-only launch principle).

  **The guest RSVP write deliberately does not use a client-side `anon` INSERT policy on `event_attendees`** (which still has zero RLS policies, same as Phase 0 left it) — the Server Action re-checks the target event is actually `published`+`public` using the request-scoped, RLS-respecting client first, then writes via the service-role client. This narrow-DTO-not-raw-table-access shape is the direct port of the Salesforce build's `GuestSubmissionController` pattern, chosen over widening RLS because a table-level policy can't restrict which *columns* an insert sets as tightly as an explicit, validated field list can.

  **A real, currently-dormant gap found while building the vendor directory, not yet fixed**: `gather_erd_master.md`'s Vendor Directory Bootstrapping & Verification section says unclaimed vendor stubs should appear in the public directory marked **Unverified** — but the actual `vendors_select_public_or_own` RLS policy from Phase 0 only lets the `anon` role see `verification_status = 'verified'` rows; an unclaimed stub is visible only to its creator and team. This doesn't matter yet because nothing can create a stub until Phase 6 ships `/vendors/add` — but Phase 6 will need to either widen that policy (`OR verification_status IN ('unclaimed', 'claim_pending')` for anon too) or deliberately revise the documented behavior. Tracked as a new Outstanding Item in `gather_web_epic_roadmap.md`, tagged for Phase 6, rather than fixed speculatively now against a policy Phase 3 doesn't own.

  **How this was actually verified**: seeded one real published/public event and one real verified vendor via the service-role REST API (not a mock), then drove the real `npm run dev` server with Playwright as a genuine anonymous browser session — confirmed both appear on Home, confirmed the vendor directory's category pill filters correctly, confirmed the RSVP form both accepts a valid submission (checked the row actually landed in `event_attendees` afterward) and rejects an invalid one server-side (a too-short name, a case the browser's own HTML5 validation wouldn't catch), and confirmed a non-existent event id correctly 404s. Zero console errors throughout. `npm run build` and `npm run lint` both pass clean.

- **2026-09-13 (Phase 4: Registration & Onboarding) — done, including proving the one thing the Salesforce build never resolved.** What shipped: `/register` (Supabase Auth `signUp()`, persona carried as a hidden field from `?persona=planner|vendor`, redirecting on success rather than returning state — same lesson as `/login`), `/register/check-email` (for a production config with email confirmation enabled, where `signUp()` returns a user but no session), `/onboarding/planner` (name/phone → `profiles`, which gained a `phone` column this phase — a genuine schema gap found comparing against the Salesforce ERD's Person Account, closed with a proper `db:generate`/`db:migrate` migration, not a manual `ALTER TABLE`), and `/onboarding/vendor` (business details → a real `vendors` insert + `vendor_team_members` Owner insert, both under RLS as the actual signed-in user via the request-scoped client, not the service role). Home's hero CTAs and the login page's "create an account" link now point at these real routes instead of Phase 3's `/login` placeholder.

  **The vendor self-registration → Owner chicken-and-egg path — the single most consequential thing the Salesforce build could never confirm actually worked for a real user — was proven end-to-end through the real UI**, not just the Phase 0 API-level test: a genuine signed-up user, through the actual onboarding form, creates a vendor and is inserted as its Owner, gated by the exact two RLS policies designed for this in Phase 0 (`vendors_insert_authenticated`, `vendor_team_members_insert_self_on_vendor_creation`).

  **Two real, genuinely subtle bugs found getting the vendor path working, neither visible from reading the code — both fully written up in `teachAndre/04-derived-state-and-router-cache.md`:**
  1. A Server Action that changes data a shared layout depends on (like personas, computed in the root layout) but doesn't itself mutate a cookie doesn't get Next's automatic "re-render the current page" treatment, and the client's Router Cache can serve a stale layout render on redirect. Fixed with `revalidatePath("/", "layout")` before the redirect in both onboarding actions.
  2. Even with fresh data reaching the client, `PersonaProvider`'s Zustand store — created once via `useState`'s lazy initializer — never re-synced when a new `personas` prop arrived later, because the root layout persists across client-side navigation rather than remounting; a freshly-onboarded vendor landed on their own dashboard still seeing the Planner tab set. Fixed by adding a `setPersonas()` store action and syncing it via a `useEffect` on every render, preserving the user's manually-chosen active persona if it still exists rather than resetting it.

  **How this was actually verified**: Playwright driving the real dev server through both complete flows — planner (register → onboard → land on an authenticated Home showing "Welcome back, \<name\>") and vendor (register → onboard → land on `/vendor/<id>/dashboard` with the Vendor tab set and a two-pill persona switcher, both confirmed by inspecting the actual rendered tab bar and switcher, not just the redirect URL) — plus direct REST checks confirming the underlying `vendors`/`vendor_team_members` rows were created correctly. Zero console errors. `npm run build` and `npm run lint` both pass clean.

- **2026-09-13 (Phase 5: Planner Core: Events) — done, including the first real collaborator invite/accept cycle exercised end to end.** What shipped: real RLS policies for `event_attendees` and `event_tasks` (owner-or-any-accepted-collaborator to read, owner-or-Editor to write — same shape as `events` itself); the real My Events list, a single-form event create/edit wizard (`/events/new`, `/events/[id]/edit` — deliberately one scrollable form rather than the Salesforce build's 4-step wizard, a UX simplification that made sense for a mobile-first Next.js page and not for the equivalent LWC screen); `/events/[id]` extended with the owner/collaborator management branch (Edit link, Attendees/Tasks links, an invite-a-collaborator form, an owner-only collaborators list) alongside the Phase 3 guest branch on the same route; `/events/[id]/attendees` and `/events/[id]/tasks` (quick-add forms, a task-completion toggle); and a first real section on the Profile stub (pending event invites with Accept/Decline) — deliberately mirroring the Salesforce build's own history, where `gatherProfile` started as exactly this same minimal stub in its Epic 5 before Epic 9 grew it into the full hub.

  Collaborator invites needed a way to resolve an email to an existing user id — `event_collaborators` has no email-invite bridge table the way `vendor_team_invites` does for vendors. Rather than add an `email` column to `public.profiles` (which would have been readable by literally anyone, guest or authenticated, under `profiles_select_all_authenticated`'s `using: true`), `findUserByEmail()` (`src/lib/supabase/service.ts`) queries Supabase Auth's admin API directly with the service role — a lookup that never touches a publicly-selectable column at all.

  **Three real bugs found, none visible from reading the schema or the page code:**
  1. **A silently-swallowed PostgREST error.** `event_collaborators` has two foreign keys into `profiles` (`user_id` and `invited_by`), which makes a plain `profiles(display_name)` embed ambiguous — PostgREST returns an error (`PGRST201`) rather than guessing. The owner's collaborators-list query wasn't destructuring `error` from the response, so the failure surfaced only as an empty list, not a visible error. Fixed by disambiguating with PostgREST's `!<constraint>` hint (`profiles!event_collaborators_user_id_profiles_id_fk`) and by actually checking `error` this time.
  2. **A real, deliberately-designed RLS policy hid the exact information a UI needed.** `is_event_collaborator()` only matches an *accepted* collaboration, so `events_select_owner_or_collaborator` correctly refused to show event details to a collaborator who hadn't accepted yet — but that also hid the event's name from the Profile "Accept/Decline" card that's supposed to help someone decide whether to accept. This is the identical gap the Salesforce build hit building the equivalent screen (its fix: adding `Event__r.Name` to `EventCollaboratorSelector.selectPendingForUser`). Fixed the same way in spirit: a new `is_event_pending_invitee()` `SECURITY DEFINER` function and an `events_select_pending_invitee` RLS policy, so a pending invitee can see the event they're being asked to help plan, without needing to accept blind.
  3. **Editing an already-applied Supabase CLI migration file's comment broke migration tracking.** A one-line comment fix to `00000000000001_profiles_on_signup.sql` (correcting a stale `db:push` reference) changed that file's checksum, which Supabase CLI's `migration up` compares against its history table — it then tried to re-apply the *entire* file and failed on `function "handle_new_user" already exists`. Reverting the edit didn't fully fix it either: Windows' git `core.autocrlf` had silently rewritten the file's line endings (LF → CRLF) on checkout, which is itself a checksum-changing edit. **Lesson: never edit an already-applied Supabase CLI migration file, not even a comment** — a wrong or outdated comment in old migration history is a cosmetic problem; a broken migration-tracking table is not. The new function this phase needed was instead applied by running its SQL directly against the database (the same recovery technique Phase 0's very first migration needed), in a brand-new migration file left untouched afterward.

  **How this was actually verified**: Playwright driving two separate real signed-up users (an owner and a collaborator, in separate browser contexts so their sessions never crossed) through the complete cycle — create event → edit → publish → add an attendee → add and complete a task → invite the second user as a Viewer → confirm the owner's collaborators list actually shows them → accept the invite from the collaborator's own Profile page → confirm the collaborator sees the management view with Attendees/Tasks access but no Edit link, matching their permission level exactly. Also re-ran the Phase 3 guest RSVP flow against the now-much-more-branched `/events/[id]` route to confirm no regression. Zero console errors throughout. `npm run build` and `npm run lint` both pass clean.

- **2026-09-13 (Phase 6: Vendor Directory & Claims) — done, and resolved the dormant vendor-visibility gap flagged since Phase 3.** What shipped: real RLS policies for `vendor_services` (public read, Owner/Manager write — no UI yet, that's Phase 8's dashboard) and `vendor_claim_requests` (visible to the claimant or the vendor's existing team, insertable by any authenticated user — approve/reject stays service-role-only, owned by Phase 10's admin console); `vendors_select_public_or_own` widened from `verification_status = 'verified'` to all three statuses, finally matching the documented community-sourced bootstrapping workflow instead of contradicting it; `/vendors/add` (search-first, a stub-creation form that only renders once a search has actually been tried); `/vendors/[id]/claim` (a claim form that shows a status message instead if the user already has one in flight, mirroring the Salesforce build's `gatherVendorClaim`); `/vendors` itself now shows every vendor (not just verified ones) with a Verified/Unverified badge, plus a "+ Add" entry point visible only to signed-in users.

  No new bugs this phase — the RLS and route work went in clean on the first pass, which is itself worth noting after three phases in a row that each surfaced a real bug: the difference this time was reusing patterns already proven out (the search-first flow mirrors `/vendors`' existing search, the claim form mirrors the RSVP/invite forms' shape, the widened policy was a straightforward `IN (...)` list change with no new cross-table logic).

  **How this was actually verified**: Playwright driving two real users — a planner who searches for a nonexistent vendor, confirms "no matches," creates a stub, confirms it immediately shows Unverified in the public directory, then confirms searching the *same* name again now finds that stub (the dedupe path actually working, not just present in the form) — and a second user who claims that stub, confirming the claim form correctly shows "pending review" on a revisit rather than letting them submit twice. Also confirmed a guest is redirected to `/login` from `/vendors/add` and never sees the "+ Add" link at all. Zero console errors. `npm run build` and `npm run lint` both pass clean.

- **2026-09-13 (Phase 7: Commerce) — done, including both DB-level business invariants that had only ever been documented intent until now.** What shipped: real RLS policies for `vendor_quotes`, `payment_plans`, and `payment_installments` (each re-deriving event/vendor-side access by walking up to `event_vendors` inline, the same shape that table's own policies already use — no new `SECURITY DEFINER` helpers needed, an EXISTS subquery per policy was enough and stays consistent with how every other cross-table check in this schema is written); a SELECT-only policy for `payment_reminders` (no UI writes these yet — reminders are an automated background-job feature with no scheduling infrastructure chosen yet, deliberately left for whichever phase picks that up); `/events/[id]/vendors` and `/events/[id]/vendors/[eventVendorId]` (quote review, accept/decline); `/events/[id]/payments` (plan creation, installments, mark-paid, accepting a `?vendor=` query param rather than becoming a third nested dynamic segment). A `formatZAR()` helper (`Intl.NumberFormat`) was added and used from the start here — the first phase where money actually renders in the UI, and the exact place the "must fix this time" bare-`R{amount}`-string item from the Outstanding Items list needed to not recur.

  **Also closed a real, necessary gap this phase's own pages exposed**: there was no way for a planner to associate a vendor with an event at all — `/vendors/[id]` gained an "Add to an event" section (a dropdown of the planner's own events), matching the Salesforce build's `gatherVendorProfile` "Associate With My Event" action. Without it, every page built this phase would have been reachable only via direct service-role seeding, never through the product itself.

  **Both commerce invariants were actually built this phase, not just documented as intended design**: the one-Accepted-quote-per-`event_vendor_id` rule as a partial unique index (`CREATE UNIQUE INDEX ... WHERE status = 'accepted'`), and the installment-sum-≤-plan-total rule as a `BEFORE INSERT OR UPDATE` trigger (`supabase/migrations/00000000000005_installment_sum_check.sql`) — a genuine cross-row aggregate check that a plain `CHECK` constraint can't express in Postgres (`CHECK` can't reference other rows of the same table), so a trigger is the correct tool here, not a workaround. The accept-quote Server Action has to explicitly decline any existing Accepted quote *before* accepting a new one — the unique index would otherwise reject the second acceptance outright rather than gracefully superseding the first, so "accept" is two sequential writes (decline-then-accept), not one.

  Stripe integration itself was deliberately not wired in this phase: the ERD's own Design Principle 9 already states "manual payment tracking today, gateway integration planned" as the current, intentional state, not a gap — and a real Stripe integration needs Andre's own Stripe account and API keys, the same category of external-account blocker as the still-pending Vercel login. `payment_installments`' `payment_gateway`/`gateway_transaction_id`/`gateway_status` columns stay reserved and unused until that's picked up.

  **How this was actually verified**: Playwright driving a real signed-up planner through the complete loop — create an event, associate an already-seeded verified vendor with it, accept a quote seeded via the service role (submission is Phase 8's job), create a payment plan, add an installment within budget, then deliberately add one that would exceed the plan total and confirm the trigger's error message surfaces cleanly in the form (not a raw Postgres error), add a valid second installment instead, mark both paid, and confirm the Total/Paid/Outstanding summary computed correctly throughout — all rendered with real ZAR currency formatting, not string concatenation. Zero console errors. `npm run build` and `npm run lint` both pass clean.

- **2026-09-13 (Phase 8: Vendor Persona) — done, and this is the phase that finally proved the live-join sharing fix and the persona switcher together, for real, with two real screens to switch between.** What shipped: real RLS for `vendor_team_invites` (visible to the vendor's team or the invitee by email, insertable by the Owner only) plus a new `vendor_team_members` self-insert policy for accepting an invite (the same chicken-and-egg shape as vendor self-registration — the accepting user isn't a team member yet, so the existing "insert by existing Owner" policy can't cover it); `/vendor/[vendorId]/dashboard` (bookings via the live `event_vendors` ↔ `vendor_team_members` join, a quotes inbox, quote submission gated to Owner/Manager, service-listing add/remove); `/vendor/[vendorId]/team` (roster, invite by email, role change, revoke — Owner-only for all four); a new "Vendor Team Invites" section on Profile (mirroring the Event Invites section from Phase 5, and mirroring the Salesforce build's own `gatherProfile` growing exactly this way).

  **`vendor_team_invites` needed a genuinely new kind of RLS helper**: unlike every other invite-shaped table in this schema (`event_collaborators`, keyed by `user_id`), this one is keyed by an email address — per the ERD, deliberately, so someone can be invited before they have a Gather account at all. RLS can't compare a column to `auth.uid()` here; it needs to know the *current user's own email*, which lives in the protected `auth.users` schema. A new `invite_email_matches_current_user()` `SECURITY DEFINER` function (querying `auth.users` internally, the first helper in this project to do so) is what makes that possible — see `teachAndre` for the connection back to Salesforce's Contact-matching-by-email pattern used for the equivalent case there.

  **The Profile page's pending-vendor-invites query needed one more piece of care than the equivalent event-invites query did**: `vendor_team_invites`' SELECT policy is an OR of "I'm already on this vendor's team" and "this invite is addressed to my email" — without excluding vendors the current user already belongs to, the Profile page would also surface invites *other people* are waiting on at a vendor the user already joined (visible via the team-member branch, but not actually addressed to them). Fixed by excluding the user's own vendor IDs from the query.

  **How this was actually verified — the single most thorough end-to-end test run of the project so far**: two real users in separate browser contexts. An Owner registers a vendor business, adds a service listing, and invites a second person (not yet registered) as Manager by email. That second person registers as a *Planner*, creates their own event, and associates the Owner's vendor with it — all *before* accepting the vendor invite, so at this point the booking exists but the invitee is not yet on that vendor's team. Only then do they accept the invite from their own Profile page. Confirmed: the Home page's persona switcher now shows both a Planner pill and the vendor's pill; switching to the vendor persona changes the tab bar to the Vendor set; and — the actual point of the exercise — the vendor dashboard immediately shows the booking created *before* this person joined the team, proving the live RLS join (not a snapshot) actually works, not just that it was written to. The same user, now acting as Manager, submitted a quote, which rendered correctly with `formatZAR()`. Zero console errors across the entire multi-actor flow. `npm run build` and `npm run lint` both pass clean.

- **2026-09-13 (Phase 9: Polish & Hardening) — done, with two items honestly left open rather than assumed closed.** This is the last phase of the originally-planned 0–9 roadmap; only Phase 10 (Admin & Support Console) remains.

  **Notification preferences**: a genuinely new `notification_preferences` table (one row per user, `email_reminders`/`sms_reminders`/`push_reminders` booleans), self-only RLS, upserted on first save rather than seeded by a signup trigger — deliberately avoiding any edit to the already-applied `profiles`-on-signup migration (see the Phase 5 lesson on why that's permanently off-limits). Wired into a real "Notifications" section on Profile, replacing the "coming soon" placeholder — SMS/push stay visibly disabled in the UI rather than pretending those channels work, matching the Salesforce build's own `NotificationSender` family, which failed loudly on those same two channels rather than faking success.

  **Accessibility**: rather than attempt a per-page audit across ~20 routes in one pass, two *systemic* fixes were made at the shared-primitive level, reaching every form and button in the app at once: `Input` now derives an `aria-label` from its `placeholder` when none is given (placeholder-only labeling fails WCAG 1.3.1/3.3.2 — it isn't reliably announced by screen readers and disappears once typing starts, a gap every form built since Phase 1 has had), and `Button`/`LinkButton` gained a `focus-visible` ring (there was no visible focus indicator anywhere in the app before this — every interactive element was genuinely untabbable-with-confidence via keyboard). Color contrast was **computed, not eyeballed**: a from-scratch OKLCH → linear sRGB → WCAG relative-luminance implementation (Björn Ottosson's published conversion matrices) found real failures — `primary` text on `bg` (3.87:1), white button text on `primary` (4.36:1), `primary` on `primary-soft` badges (3.28:1), and `success` green text on `bg` (2.07:1, a serious failure). None of this app's actual text is large enough (≥18.66px bold or ≥24px) to use the looser 3:1 AA threshold, so 4.5:1 is the real bar everywhere it's used for body text. **Deliberately not fixed**: these are the confirmed, stakeholder-approved brand colors ported from the approved prototype, and adjusting a brand color is a design decision, not a technical one — flagged in Outstanding Items for an explicit human call instead.

  **Vitest + React Testing Library, from scratch**: `vitest@3` (not the latest v5 — v5's peer dependency on `@types/node@^22` conflicted with this project's existing `^20` pin, and bumping that just to satisfy a test runner's peer range wasn't worth the ripple risk) plus `@testing-library/react`/`user-event`/`jest-dom`, configured per Next.js's own official Vitest guide (`vite-tsconfig-paths` + `@vitejs/plugin-react`, `jsdom` environment). That same guide states plainly: **Vitest does not support async Server Components at all** — and every `page.tsx` in this entire project is one, fetching data. This isn't a gap to work around; it's the documented reason this project's Playwright-driven manual verification each phase *is* the correct testing strategy for pages, not a stand-in for "real" tests. Vitest/RTL's actual scope here is what it's genuinely good for: the shared UI primitives (`Button`, `LinkButton`, `Input`) and pure functions (`formatZAR`, `sastInputToIso`/`isoToSastInput` round-tripping, `cn`) — 16 tests, all passing, including regression coverage for both accessibility fixes made this same phase (so a future edit that silently removes the aria-label derivation or the focus ring gets caught immediately, not rediscovered by testing manually). `vitest.config.ts` had to be excluded from the root `tsconfig.json` — `next build`'s typecheck pass has a broad `**/*.ts` glob that was pulling in a duplicate-Vite-version type conflict (this project's own Vite via `@vitejs/plugin-react` vs. one nested inside `vitest`'s own `node_modules`) that had nothing to do with the app itself.

  **Mobile verification**: Playwright's real device presets (`devices['iPhone 13']` — correct viewport, device pixel ratio, touch, UA) replaced the plain-390px-viewport testing used every prior phase, and caught one thing worth a second look: an apparent tab-bar/content overlap on `/vendors` that turned out to be a false positive from checking overlap at the *initial* scroll position, where a `position: fixed` nav always visually sits over whatever's currently on-screen — confirmed harmless by explicitly scrolling to the actual bottom and re-measuring (the last card cleared the nav by 54px). **What this still can't do**: validate real `env(safe-area-inset-*)` behavior, which is a WebKit/iOS Safari-specific feature Chromium doesn't faithfully replicate even in device-emulation mode — the architecture doc's Verification section has named this gap since Phase 2, and it remains open, needing Andre's own phone.

  **How this was actually verified**: real Playwright runs (not device presets initially, then device presets specifically for the mobile pass) confirming notification preferences persist correctly across a reload (checked → unchecked → reload → still unchecked), `npm test` running all 16 Vitest tests clean, and the device-preset pass across Home/Vendors/Login/Register catching zero real layout bugs (one false positive, investigated and ruled out). `npm run build` and `npm run lint` both pass clean.

- **2026-09-14 (Design system v2: multi-theme + motion redesign) — a full visual/UX pass across every existing route, done after Phase 9, not a new numbered phase.** Triggered by direct feedback that the app "felt bland" next to the confirmed prototype and a (subsequently disproven) memory of a 3-theme switcher in `gather-bold-playful.html` — a full read of that file confirmed it never had one; the two new themes below were designed from scratch rather than recovered.

  **Theme system**: two new palettes, **Ocean Current** (cool blues/teals) and **Sunset Social** (warm orange/pink), added alongside the existing **Bold Playful** (magenta/gold) as `:root[data-theme="…"]` overrides in `globals.css`, same token shape (bg/surface/ink/text/text-muted/border/primary/primary-soft/secondary/secondary-soft/success/success-soft) plus one new token, `--color-primary-glow` (the gradient's second stop — the prototype hardcoded this per-theme, which wouldn't have worked for Ocean Current/Sunset Social). `:root[data-theme]`'s higher specificity `(0,0,2,0)` reliably beats the bare `:root` block regardless of source order, and — structurally — these blocks only ever declare `--color-*` custom properties, never a real rendered property on a bare element selector, so they can't reintroduce the Phase-mid-session `@layer` bug (an un-layered rule beating a Tailwind utility class). Selection persists **per-device via `localStorage`** (`src/lib/stores/theme-store.ts`) — a **plain module-singleton Zustand store**, deliberately unlike `persona-store.ts`'s per-request Context store: personas are session-derived and differ per request (a singleton would leak across users), while theme is never derived from the session at all — every server render produces the same hardcoded default for every user, so a singleton has nothing request-specific to leak. Flash-of-wrong-theme is avoided with a small blocking inline `<script>` in `layout.tsx`'s `<head>` that reads `localStorage` and sets `data-theme` before first paint, paired with `suppressHydrationWarning` on `<html>` — React's sanctioned, attribute-only escape hatch for exactly this case. **Hard constraint going forward**: no component may branch rendered JSX on the active theme (only on CSS variables) — doing so would diverge between the server's hardcoded default and the client's real localStorage value on first hydration and throw a real, non-suppressed hydration error, unlike the shallow attribute mismatch the inline script causes.

  **Motion**: added `motion` (the current unified Framer Motion package) for spring-physics interaction rather than hand-rolled CSS — a spring can be interrupted and reversed mid-bounce on rapid tap/release, which a fixed-duration CSS transition can't do without a visible snap. `Button`/`LinkButton` and `Card`'s new `LinkCard` sibling both needed `"use client"`: not just because rendering a motion component requires it (the library's own render-layer files already carry `"use client"`, so that part composes fine under a Server Component parent), but because **`motion.create(Link)` itself — the factory call, at module-evaluation time — is client-only code**, confirmed by a real build failure (`Attempted to call createMotionComponent() from the server`) the first time `card.tsx` tried to stay a plain server component while still defining `LinkCard` in the same module. New shared primitives: `src/components/motion/stagger-list.tsx` (`StaggerList`/`StaggerItem`, staggered fade+rise entrance, used across every list-rendering page — events, vendors, attendees, tasks, payments installments, bookings/services, team members/invites — replacing the previous plain `.map()` output), a root `src/app/template.tsx` (remounts per navigation, giving a free per-route fade+rise transition without depending on Next 16's still-experimental View Transitions flag), and `src/lib/confetti.ts` (dynamically imports `canvas-confetti`, reads the *active theme's own* colors via `getComputedStyle` rather than hardcoding any, and no-ops under `prefers-reduced-motion: reduce`) — wired to two success moments: guest RSVP (`rsvp-form.tsx`) and quote acceptance. The quote-accept action (`events/[id]/vendors/[eventVendorId]/actions.ts`) needed a real refactor, not just a UI wrapper: it was a bare `<form action={acceptQuote}>` with no return value, so there was no definite "it succeeded" signal to hook confetti into. Converted to `useActionState` (mirroring the established `submitRsvp`/`inviteCollaborator` pattern) — and, per the lesson already documented for `inviteCollaborator`, **deliberately dropped the action's own `revalidatePath` call in the same change**, since bundling a server-triggered re-render into a `useActionState` action's response risks discarding its `{success: true}` before the client reads it. The other quotes' declined status (from the mutual-exclusivity write) catches up on the next normal reload, the same accepted tradeoff already made for `inviteCollaborator`.

  **Landing page**: the guest branch of `src/app/page.tsx` was extracted into a new Client Component, `src/components/landing/guest-landing.tsx`, receiving the page's already-fetched `events`/`vendors` data as props rather than duplicating the queries — a gradient hero (using the new `--color-primary-glow` token) with an inline-SVG decorative circle texture (cheap, crisp, recolors for free under any theme, unlike an image asset), 3 feature-highlight rows using `lucide-react` icons (the project had **zero** icon components of any kind before this), and a horizontally-scrolling vendor showcase. Ported the prototype's verified/unverified vendor gradient convention (`src/lib/vendor-gradient.ts`) generalized to hash any free-text `primary_category` into a stable hue rather than a hardcoded lookup table, since `vendors.primary_category` has no fixed enum.

  **New dependencies**: `motion`, `canvas-confetti` (+ `@types/canvas-confetti`), `lucide-react`. `next.config.ts` gained `experimental.optimizePackageImports: ["lucide-react", "motion"]`.

  **How this was actually verified**: `npm run build` and `npm test` (all 16 pre-existing Vitest tests, unaffected) both pass clean. A real Playwright pass against the running dev server + local Supabase stack confirmed: the guest landing hero renders with real motion and no console errors; theme switching from `/profile` updates `data-theme` immediately and screenshots visually confirm all 3 palettes render correctly across Home/Events/Vendors/Profile; a hard reload after switching to Ocean Current showed the correct theme already applied in the very first post-navigation screenshot (no flash of the default Bold Playful); a full signup → create event → RSVP-as-a-guest flow fired confetti in the *active* theme's own colors (verified visually under Sunset Social); and `prefers-reduced-motion: reduce` (via Playwright's `emulateMedia`) correctly resolved the shimmer keyframe's `animation-name` to `none` on a real primary button.

- **2026-09-14 (Mobile testing round 1: 4 targeted UX fixes) — the first real feedback from actually using the app on a phone, not reading code.** Also surfaced and fixed, as a prerequisite to testing at all: Next.js 16 blocks cross-origin requests to dev-only assets by default (a phone hitting the dev server over its LAN IP is a different origin than `localhost`), so `next.config.ts` gained `allowedDevOrigins: ["<lan-ip>"]` — without it every JS chunk request is silently blocked and the page never hydrates past its raw server-rendered shell, which is exactly the "blank page, tab bar renders but nothing else does" symptom that showed up first. This is a per-network setting (the LAN IP needs updating if the dev machine's address changes) — flagged as a real recurring friction point, not fixed for good.

  Four feedback items, all fixed against existing infrastructure with no schema/RLS changes needed:
  1. **Events list and detail page couldn't distinguish "events I own" from "events I'm invited to."** `src/app/events/page.tsx`'s query never selected `owner_id` or fetched the current user, even though `events_select_owner_or_collaborator` RLS already returns both kinds in one list. Fixed by selecting `owner_id` and adding `supabase.auth.getUser()`, then a **"Hosting"/"Invited"** badge (deliberately not "Owner"/"Collaborator," to avoid colliding with the existing `permission_level` vocabulary already shown in the collaborators list). Same badge, computed from the already-fetched `access` object, added to the event detail header (`"Hosting"` / `"Editor"` / `"Viewer"`, owner-first since `access.isEditor` is `true` for owners too).
  2. **Payment-plan creation had no discoverable entry point** — the only path was event detail → Vendors → a specific vendor → its "Manage Payments" link. Added a **Payments** link to the event detail page's own nav row, converting it from a 3-item `flex` row to a `grid grid-cols-2 gap-2` (avoids squeezing 4 labels into `flex-1` thirds on a `max-w-sm` screen). The vendor-picker landing page's copy was also improved for being reached cold now: a real explanation instead of "Pick a vendor," and the zero-vendor dead-end ("No vendors associated with this event yet.") became an actual `LinkButton` to `/vendors` instead of just naming the directory in unlinked prose.
  3. **Two inputs had no real visible label** — the RSVP form's guest-count input (a wrapping `<label>` existed, but read as loose inline text next to a tiny `w-20` box showing "0") and the task due-date input (`type="date"`, no label at all — native date inputs mostly ignore `placeholder`). Root cause: the codebase already had two different hand-rolled stacked/inline label patterns plus one field with no label, so patching just these two would have made a fourth. Extracted the better of the two existing patterns into a real primitive, `src/components/ui/field.tsx` (`Field`, a verbatim lift of `event-form.tsx`'s own `flex flex-col gap-1 text-xs font-extrabold text-text-muted` stacked-caption classes), and applied it at all four sites that needed it: `rsvp-form.tsx`'s guest count, `add-task-form.tsx`'s due date, `event-form.tsx`'s own 3 pre-existing labels (so there's one implementation instead of the primitive plus a parallel duplicate), and — found while fixing the others, same exact bug, not originally reported — `add-attendee-form.tsx`'s guest-count input, which had a `placeholder` that could never actually show since `defaultValue={0}` always fills the field.
  4. **No way for the owner/editor to manually record an attendee's RSVP** (e.g., someone texted them instead of using the public RSVP form). Confirmed no RLS/schema gap first: `event_attendees_update_owner_or_editor` already permits UPDATE by the owner or an editor collaborator — only the Server Action and UI were missing. Added `updateAttendeeRsvp` to the existing `attendees/actions.ts` (plain `Promise<void>`, no `useActionState`, matching `task-toggle.tsx`'s reasoning: a closed-vocabulary control, not a form reporting field errors) and a new `rsvp-select.tsx` — a native `<select>` (not custom pill buttons, so it's a same-footprint swap for the existing status pill rather than growing every row, and it opens the OS's own large mobile picker) that auto-submits on change via `formRef.current?.requestSubmit()` inside a `useTransition` for a pending state. Shown only when `access.isEditor`; everyone else still sees the original read-only pill.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) both pass clean. A real two-browser-context Playwright pass (an owner and a separately-invited-and-accepted collaborator, matching this project's established multi-actor verification style) confirmed: the owner's events list and event detail both show "Hosting" while the collaborator's show "Invited"/"Editor"; the Payments nav link reaches the payments page directly from event detail; both the guest-count and due-date fields render their new visible captions; and, as the owner, setting an attendee to "attending" via the new `<select>` (without ever touching the public RSVP form) persisted correctly across a reload. Zero console errors throughout.

- **2026-09-14 (Mobile testing round 2: back navigation, an empty-state dead end, and a real ownership-model mixup) — round 1's fixes prompted more actual phone use, which found 3 more issues.**
  1. **No in-app way to navigate back** — every drill-down page (anything not one of the 4 tab-bar roots) had zero back affordance beyond the OS/browser gesture, confirmed by grepping the whole `src/` tree for any back-navigation pattern and finding none. Added one shared primitive, `src/components/ui/back-button.tsx` (`router.back()`, not a hardcoded parent `href` — it returns to wherever the user actually came from, e.g. a filtered vendor search), and applied it to all 12 non-root drill-down pages (event detail/edit/new, attendees, tasks, event-vendors list/detail, payments, vendor profile/claim/add, vendor team). `/vendor/[vendorId]/dashboard` deliberately excluded — it's a tab-bar root for the vendor persona, not a drill-down page.
  2. **The event-scoped Vendors page's empty state named the vendor directory in prose with no actual link** — same gap already fixed on the Payments page's empty state in round 1, just missed here. Added a real "Add a vendor" `LinkButton` to `/vendors`.
  3. **A real product misunderstanding, caught early because a real person used the actual flow, not just code review**: the Payments empty-state copy ("Pick a vendor to manage payments for") read as if the vendor was involved in managing the plan, which risked getting genuinely confused with `vendor_quotes` (which *is* vendor-managed, planner-approved). Checked the actual RLS first, since this is exactly the kind of thing to verify rather than assume: `payment_plans_insert_event_editor`/`payment_plans_update_event_editor` already have zero vendor-side branch (confirmed via the policy's own pre-existing code comment, "Setting up a payment plan is a planner action only") — so the *implementation* was already correct, only the *copy* was ambiguous. Reworded both payment-plan screens to say the plan is the planner's own to set up and maintain and that the vendor never sees or edits it, and — since this is a rule easy to accidentally invert later without a written anchor — added an explicit "Quotes and payment plans have deliberately opposite ownership" rule to Business Rules & Invariants below, plus a new Product Design Principle: early on, most planners self-manage with little or no vendor-side participation, so planner-facing tracking features must work fully standalone rather than assuming the vendor is ever using Gather at all.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) both pass clean. A real Playwright pass confirmed the back button renders and correctly returns to the previous page (not just a hardcoded parent) from the event-vendors page, the new "Add a vendor" button is present and links to `/vendors`, and the payments page's reworded copy renders as expected. Zero console errors.

- **2026-09-14 (Phase 10: Admin & Support Console) — built exactly to the design in `docs/gather_web_admin_architecture.md`, the last phase of the roadmap.** What shipped: `profiles.is_admin` (new column, flipped by hand only) plus `is_admin(uuid)` (`supabase/migrations/00000000000007_is_admin_function.sql`, applied via the established direct-`postgres`-script method, not `supabase migration up` — see the Phase 5 migration-immutability lesson for why); `src/lib/admin/require-admin.ts` (the `notFound()`-not-redirect guard, called at the top of every `/admin` page and every admin Server Action, no exceptions); `src/lib/admin/audit-log.ts` (`logAdminAction`, written by every mutation); three new service-role-only tables (`support_cases`, `support_case_comments`, `admin_audit_log`); and all 10 routes from the doc's Route Inventory, including the claim-approval queue (`/admin/vendors/claims`) that finally builds the approve/reject action `vendor_claim_requests` was left waiting for since Phase 6 — provisioning the claimant as Owner, verifying the vendor, and auto-rejecting competing pending claims in one Server Action, the same multi-write-not-a-transaction shape already used for `acceptQuote`. Also added, since it was documented as a rule but never actually buildable until an approve action existed: the `vendor_claim_requests_one_approved_per_vendor` partial unique index.

  **The one real deviation from the written design**: the doc originally left open exactly *how* `/admin` would get its own non-mobile layout. Building it surfaced the real choice — Next's framework-native answer (a second root layout via route groups, its own `<html>`/`<body>`) would have meant moving every existing consumer route into a route group first, a large refactor with no product benefit. Went with a smaller, equivalent fix instead: `src/components/app-shell.tsx` (already a Client Component) branches on `usePathname()`, skipping the mobile tab bar/persona-switcher for any `/admin` path and rendering `{children}` directly, with `/admin/layout.tsx` supplying its own sidebar. Same single root layout, same fonts/tokens, no risk of the "full page reload between different root layouts" caveat Next's own route-groups doc calls out. Documented in `gather_web_admin_architecture.md`'s UI & Navigation section so this doesn't read as an oversight later.

  **How this was actually verified**: `npm run build` (all 12 new admin routes compiled) and `npm test` (16/16) both pass clean. A real Playwright pass: a signed-up non-admin planner hitting `/admin` got a genuine 404 (not a redirect, confirming `requireAdmin()`'s `notFound()` choice actually works, not just reads correctly in the source); the flagged test admin account could open the dashboard (real counts, sidebar, no mobile chrome), edit a planner's profile (persisted, audit-logged), open an event and cancel it, and create a support case → comment on it → change its status — with the dashboard's Recent Activity feed correctly showing `case.created`, `case.status_changed`, and `profile.edited` audit rows afterward, proving the audit-log write path end to end rather than just trusting the code. One pre-existing, unrelated bug found along the way and deliberately **not** fixed: any `notFound()` page in the app (not admin-specific) logs a harmless "Encountered a script tag while rendering" React warning, traced to the root layout's flash-of-wrong-theme-prevention inline `<script>` (added in the design-system-v2 redesign). Tried the obvious fix (`next/script` with `strategy="beforeInteractive"`) and reverted it immediately — it made the actual flash-of-wrong-theme bug it was meant to prevent come back, since `beforeInteractive` only guarantees "before hydration," not "before first paint." Left as a known, console-only, cosmetic issue rather than trade a real visible bug for a devtools warning.

- **2026-09-14 (Full project code review) — a multi-angle review of everything built to date, run against real code, not just a design pass. Found one real, shipped security vulnerability and five real correctness/accessibility bugs, all fixed and re-verified; a further set of duplication/efficiency findings were logged as follow-up rather than fixed immediately, to keep this pass proportionate.**

  **Security — the most serious finding**: `is_admin(p_user_id uuid)` (Phase 10) accepted an arbitrary target id and was granted to `anon`. Since `profiles_select_all_authenticated` already lets anyone enumerate every profile id, a client could loop `rpc('is_admin', { p_user_id: id })` over every id and reconstruct the exact admin roster the function existed to hide — independently found and confirmed by three separate review passes. The fix had two parts, because there were two separate holes: (1) `is_admin()` now takes no argument and checks `auth.uid()` internally (`supabase/migrations/00000000000008`) — it's now structurally impossible to ask about anyone but yourself; (2) direct column access to `is_admin` was revoked outright (`00000000000009`), because Postgres RLS is row-level only — the SECURITY DEFINER function was never sufficient by itself, since the raw column was always readable via PostgREST directly, bypassing the app and the RPC entirely. **A genuine Postgres subtlety hit fixing part 2**: a first attempt (`revoke select (is_admin) ... from anon, authenticated`) executed with no error but had *zero* effect — verified directly against `information_schema.column_privileges`. The reason: a column-level `REVOKE` cannot carve an exception out of the broader table-level `grant all` this project's tables already had (`00000000000003_restore_public_grants.sql`) — the table-level grant simply wins. The actual fix is the reverse order: revoke the table-level `SELECT` entirely, then `GRANT SELECT` back on only the specific columns (`id, display_name, phone, created_at`) that should stay readable. Re-verified against a real `SET ROLE anon` session afterward, both that `is_admin` is now blocked and that legitimate columns still aren't.

  **Five correctness/accessibility bugs, all confirmed against real behavior (not just read from the diff) and fixed**:
  1. **`acceptQuote` accepting a quote left a stale, still-functional "Decline" button on screen that could silently revert the acceptance** — the action's own `revalidatePath` was deliberately dropped earlier to avoid clobbering `useActionState`'s return value, but nothing replaced it, so the page's server-rendered `quote.status === "sent"` check (gating both Accept and Decline) never updated. Fixed with a client-triggered `router.refresh()` in `AcceptQuoteForm` after a confirmed success — this runs *after* `useActionState` already committed the result, so it doesn't hit the original clobbering bug — plus a `.eq("status", "sent")` guard added to both `acceptQuote`'s and `declineQuote`'s own updates as the real underlying invariant, not just a UI-timing fix. Verified live: before the fix, Decline stayed clickable after Accept; after, the whole page (including the status pill) updates within the same interaction and Decline disappears, confirmed again after a hard reload.
  2. **`approveClaim` (the vendor-claim queue) discarded the `{error}` result of 3 of its 4 writes**, so a failed `vendor_team_members` insert would still log `vendor_claim.approved` as a clean success while the documented invariant ("a claimant becomes Owner the moment their claim is approved") silently didn't happen. Now checks each write: a failed Owner-provisioning insert reverts the claim to `pending` and logs `vendor_claim.approve_failed` instead of continuing; a failed verification-status update (lower severity — Owner is already provisioned, and the vendor detail page's own "Mark Verified" button already covers manual recovery) logs `vendor_claim.verify_failed` but doesn't block the rest.
  3. **`updateAttendeeRsvp` (the manual RSVP editor) discarded its own write result, and `RsvpSelect` was an uncontrolled `<select>`**, so a failed or RLS-filtered update (zero rows affected, no Postgres error at all) looked identical to a successful one — the browser had already shown the newly-picked value regardless of what the database actually did. `RsvpSelect` rewritten as a controlled component that calls the action directly (not via a hidden `<form>`) inside a transition, reverts to the previous value and shows a toast on any failure — including the zero-rows-affected case, now detected via `.select("id")` on the update and checking the returned array is non-empty.
  4. **A real WCAG regression from combining two otherwise-correct pieces**: `Input`'s existing placeholder→`aria-label` default (Phase 9) and the new `Field` component (mobile-testing round 1) fought each other — per the ARIA accname algorithm, an `aria-label` attribute always wins over a wrapping `<label>` element, so any `Field`-wrapped `Input` that also had a `placeholder` announced the placeholder text instead of the real field label (concretely: the admin new-case form's requester-email field). Fixed with a `deriveAriaLabelFromPlaceholder` prop on `Input`, defaulting to `true` everywhere except when `Field` wraps it (via `cloneElement`, checking `children.type === Input` so it doesn't touch the `<select>` fields `Field` also wraps). Verified: the input's own `aria-label` attribute is now absent, and its real associated `<label>` text is what a screen reader will actually announce.
  5. **A visual regression from the earlier `Link`→`LinkCard` motion rollout**: Home's authenticated-view vendor chips (`<LinkCard className="rounded-pill ...">`) rendered with both `LinkCard`'s own `rounded-[22px]` base radius *and* the `rounded-pill` override still present, plus the unwanted card padding/shadow — `tailwind-merge` has no built-in knowledge of this project's custom `rounded-pill`/`rounded-field` theme extensions, so it never recognized them as conflicting with `rounded-[22px]` in the same "border radius" group. Fixed at the root, not just the one call site: `cn()` (`src/lib/utils.ts`) now uses `extendTailwindMerge` to register both custom radius values, verified directly (`twMerge("rounded-[22px] p-4", "rounded-pill px-3")` now correctly collapses to just the later classes) so this can't silently recur elsewhere. The one call site itself was also reverted from `LinkCard` to a plain `Link` — it's a small pill tag, not a card, so `LinkCard`'s base classes never belonged there regardless.

  **Also fixed, lower-severity**: a fragile positional-index array in the admin dashboard's stat cards (relabeled by zipping label+count together directly, so reordering either list can't silently misattribute a count); an unnecessary `Promise.all` in `confetti.ts` wrapping one real async call and one synchronous one.

  **Deliberately not fixed this pass, logged as Outstanding Items instead**: significant JSX duplication across the new admin console (a "count header + list-or-empty-card" section repeated ~14 times across the 3 detail pages, a search-form-plus-query-builder pattern repeated 3 times across the list pages, badge-class lookup tables not shared) — a real, multi-agent-confirmed finding, but refactoring it is a proportionate follow-up task, not something to fold into an already-large fix pass. Also logged: missing `.limit()` caps on several admin detail-page sub-queries, `requireAdmin()` re-querying per request rather than being wrapped in React's `cache()`, and a few serial `await`s in admin detail pages that could be `Promise.all`'d.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) both pass clean after every fix. Each security fix was verified against the actual Postgres privilege system directly (`SET ROLE anon`/`authenticated`, not just reading the SQL and assuming it works) — both the old vulnerable RPC signature and a direct PostgREST column read now correctly fail. Each behavioral fix was verified with a real Playwright flow reproducing the exact bug first (confirmed broken), then confirming the fix (confirmed working), rather than trusting the diff alone — including a full vendor-quote accept flow with two real accounts and a hard reload to confirm the fix isn't just a client-side illusion.

- **2026-09-14 (Upcoming Tasks/Payments dashboard, per-event pending markers, an expandable verification badge, and a vendor test account) — direct feedback from continued manual use.** What shipped:
  1. **A new per-user preference**: `notification_preferences.upcoming_window` (`on_day` / `one_day_before` / `one_week_before`, default the most generous option), surfaced as "Show upcoming tasks & payments" on Profile's Notifications & Reminders card. Deliberately a *display-filtering* preference, not a notification-sending one — kept on the same table as the existing email/sms/push toggles since it's still a per-user "how do you want reminders to work" setting, but it doesn't depend on (or block on) the still-undecided background-job scheduler for actually *sending* reminders. `src/lib/upcoming.ts` centralizes the window→cutoff-date math so Home and My Events compute "upcoming" identically.
  2. **Home gained two new sections** for authenticated users, "Upcoming Tasks" and "Upcoming Payments" — scoped to the signed-in user's own events (owner or collaborator; RLS does the actual scoping, same policies Phase 5/7 already built), unlike the "Upcoming Events"/"Vendors" sections already on that page, which have always shown the *public* directory content, not personal data. Payments required a 3-level PostgREST embed (`payment_installments → payment_plans → event_vendors → events`/`vendors`) to get from an installment back to a displayable event/vendor name — the same embed-chaining style already used on the admin vendor detail page, just one level deeper.
  3. **My Events gained small pending-task/pending-payment badges per event card** (`ListTodo`/`Wallet` icons from `lucide-react`), computed as two `Set<eventId>` lookups from two queries (not N+1 per-card queries) using the same preference/cutoff helper.
  4. **The Verified/Unverified badge became an icon** (`BadgeCheck`/`ShieldQuestion`) that expands inline on tap to explain what it means — but only on the vendor detail page (`src/app/vendors/[id]/page.tsx`), not on any vendor list row. The list rows (`/vendors`, `/vendors/add`) already wrap the whole row in a `LinkCard`, and nesting a clickable `<button>` inside that `<a>` would be invalid HTML — `src/components/vendor/verification-badge.tsx` has an explicit `interactive` prop (default `false`) specifically so list usage stays a plain, safe `<span>` while the standalone detail page gets the real expandable button.
  5. **A vendor test account** (`vendortest@gather.dev`, in `TESTING.md`) — there wasn't one to test the Vendor persona with before this.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass: created a task due tomorrow and confirmed it appeared on Home and as a My Events badge under the default one-week window, then narrowed the preference to "on the day" and confirmed *both* disappeared (proving the preference actually drives both surfaces, not just one); created a real payment plan + installment through the actual UI (not seeded directly) and confirmed the same for Upcoming Payments; confirmed the interactive verification badge expands with the right explanation on the vendor detail page; confirmed the new vendor account can sign up, onboard, log out, and log back in through the real form.

- **2026-09-14 (Persona switcher relocated to Profile; verification badge made icon-only and interactive everywhere, including list rows) — same-day follow-up feedback after trying the above.**
  1. **The persona-switcher pill row moved from a permanent fixture at the top of every authenticated page into a new "Switch Persona" section on Profile** (shown only when `session.personas.length > 1`, same condition as before — just relocated, not newly conditional). `AppShell` no longer renders `<PersonaSwitcher />` above `<main>`; `PersonaSwitcher` itself only needed its own wrapper's padding/overflow classes adjusted (it was styled for a full-bleed top bar, not an element inside a `Card`) — the store/context wiring (`PersonaProvider` still wraps the whole authenticated tree) needed no changes at all, since Profile is just another page rendered inside that same provider.
  2. **The Verified/Unverified badge became icon-only** (dropped the visible "VERIFIED"/"UNVERIFIED" text label, keeping it only in `aria-label`) **and gained real tap-to-expand/collapse everywhere**, including vendor list rows (`/vendors`, `/vendors/add`) — the previous round had deliberately kept list rows non-interactive specifically because they were each a whole-row `LinkCard`, and a `<button>` can't nest inside a `<Link>`'s `<a>`. Fixed properly this time by restructuring those rows: a plain `Card` (not `LinkCard`) with the vendor name as its own inner `<Link>`, and the badge sitting beside it as a sibling `<button>` — not nested inside anything, so it's finally real, valid, interactive HTML on every row, not just the standalone detail page.
  3. **A real layout bug found and fixed while verifying #2, not shipped**: the first version of the expand-in-place badge overlapped the vendor's name with the explanation paragraph once expanded, because a `flex items-center justify-between` row has no way to let one flex item's overflow wrap onto its own line — the button+paragraph, bundled in one flex item, either stayed one squashed inline box or dragged the button down with the text. Fixed with `display: contents` (Tailwind's `contents` utility) on the badge's own wrapper, which removes that wrapper's own box entirely so the button and the (conditionally-rendered) paragraph become direct flex children of the *row itself* — then `order-last basis-full` on just the paragraph makes it alone claim the full row width and wrap, while the button stays put at its normal small size. Requires the parent row to have `flex-wrap`, added to both list pages' `Card` className.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass confirmed: no persona pills render at the top of any page anymore; Profile shows "Switch Persona" for a real multi-persona account; the vendors list shows no literal "VERIFIED"/"UNVERIFIED" text anywhere; clicking a badge on a list row expands the explanation and clicking again collapses it; tapping the vendor *name* (not the badge) on that same row still navigates correctly to the detail page — proving the restructured row didn't just fix the HTML nesting but kept the actual navigation working; and a fresh screenshot after the `display: contents` fix shows the explanation wrapping cleanly onto its own line with zero overlap, compared to the broken version caught before shipping.

- **2026-09-14 (Show/hide password toggle) — a small, targeted addition.** `src/components/ui/password-input.tsx` wraps `Input` (the same "small dedicated wrapper" shape as `Field`, not new logic bolted onto `Input` itself, which stays the plain component every non-password field still uses) with an eye/eye-off icon button that flips the field between `type="password"` and `type="text"`. Wired into both `LoginForm` and `RegisterForm` — the request named login specifically, but the identical UX problem (no way to check what you actually typed) exists at signup too, and it's the same one-line swap at both call sites. Verified with a real Playwright pass: toggling actually changes the input's `type` attribute (not just the icon), toggling back reverts it, and it works identically on both forms.

- **2026-09-14 (Verification badge shows the vendor's own description; vendor social links + two photo galleries — this project's first use of Supabase Storage) — the biggest single feature addition since Phase 10.** What shipped:
  1. **`VerificationBadge`'s expand now shows the vendor's own description** (with a small "Verified"/"Unverified" label prefix) instead of a generic, one-time-useful explanation of what the badge means — a direct improvement on the previous round's design once it was actually used.
  2. **Confirmed, not assumed**: `/vendors/[id]/page.tsx` already is the planner-facing vendor detail page (name, category, description, phone/website, the verification badge, "add to event") — this phase extended it rather than building a new one.
  3. **`vendor_social_links`** — a freeform platform+URL table (not an enum; "other social media tags" was the actual ask), public to read, insert gated to owner/manager **and** a verified vendor. Managed from the vendor dashboard, displayed on the public vendor profile.
  4. **Two new Supabase Storage buckets** — `vendor-gallery` (public read, since a vendor's gallery is marketing content) and `event-gallery` (private, since a planner's mood-board photos are personal planning content — display goes through a server-generated signed URL, never a permanent public one). Both gated by `storage.objects` RLS policies parsing the vendor/event id out of the upload path via Supabase's own `storage.foldername()` helper — mirroring the exact same owner/collaborator (event) and owner-or-manager-if-verified (vendor) rules already enforced at the table level, since neither layer protects the other on its own.
  5. **A new event sub-page, `/events/[id]/gallery`** — upload/caption/delete reference photos per event, owner/editor-collaborator only, added to the event detail nav row.
  6. **Upload is a Server Action receiving the file via `FormData`** (Server Actions can read a `File` directly, no separate upload endpoint needed), using the request-scoped client for both the Storage write and the DB insert — RLS is the actual enforcement, not the action's own logic, consistent with this project's standing preference for RLS-first authorization over service-role bypasses wherever avoidable.

  **A real, non-obvious bug found and fixed while building this, not shipped broken**: the first version of the vendor-gallery upload policy inlined `EXISTS (SELECT 1 FROM vendors WHERE id = ... AND verification_status = 'verified')` directly in the `storage.objects` policy's `WITH CHECK` — and it reliably evaluated to `false` even for a genuinely verified vendor's own owner, confirmed visible to that same user via a normal PostgREST `select` moments earlier. Isolated by testing each clause of the policy in isolation directly against the running local stack (not by reading the SQL and guessing): `bucket_id` check alone passed, `auth.uid() is not null` passed, `is_vendor_team_member(...)` alone passed, the inline `vendors` `EXISTS` clause alone failed — a nested-RLS-across-schemas quirk specific to a policy evaluated from within Storage's own policy context, not something that shows up on an ordinary table's own RLS (the identical inline `EXISTS` on `vendor_gallery_images`' own table-level INSERT policy worked correctly). Fixed by extracting it into `is_vendor_verified(uuid)`, a `SECURITY DEFINER` function — the exact same principle this project has used since Phase 0 for every other cross-table RLS check, just not initially applied here. Used consistently everywhere now: both storage policies and both table-level policies (`vendor_social_links`, `vendor_gallery_images`) call the function instead of inlining the subquery.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass, including reproducing the storage bug live before fixing it (not just in isolated SQL): an unverified vendor's dashboard correctly hides both the social-link and gallery upload forms behind an explanatory message; after an admin marks the vendor verified, both forms appear and actually work — a real Instagram link and a real uploaded photo (a genuine multipart file upload through the browser, not seeded directly) both show up on the public vendor profile; a planner created a real event, uploaded a captioned reference photo to its private gallery, and saw it render via a signed URL. Left the storage bucket/table state as it should be: a stray API-only test script's debug rows and orphaned Storage objects (created while isolating the RLS bug) were found and removed afterward via the real Storage API, not a raw table delete — Postgres itself has a trigger blocking direct `DELETE` on `storage.objects` specifically to prevent exactly that kind of orphaned-file mistake.

- **2026-09-14 (Client-side gallery compression, a 20-photo cap on both galleries, and a real display treatment for the public vendor profile) — a direct cost/UX follow-up once it became clear an uncompressed, uncapped gallery could grow Storage volume/egress unpredictably.** What shipped:
  1. **`src/lib/image-compression.ts`** — a Canvas API resize/re-encode (`createImageBitmap` → downscale so the longest edge is at most 1600px → re-encode as JPEG at quality 0.82), no external library. Runs client-side, inside the action passed to `useActionState` (see the gotcha below), before the file ever reaches the Server Action. Falls back to the original file on any decode failure or if the "compressed" result would actually be larger. Measured on a real 4000×3000 synthetic test photo: 7.2MB in, ~312KB uploaded — a ~96% reduction, with no visible quality loss at gallery-thumbnail/carousel size.
  2. **A shared `MAX_GALLERY_IMAGES = 20` cap on both galleries**, enforced with a plain count-then-check in each upload action (`vendor/[vendorId]/dashboard/actions.ts`, `events/[id]/gallery/actions.ts`) — a soft product/cost-control limit, not a data-integrity invariant like the payment-installment-sum trigger, so no DB constraint was warranted. Both dashboard pages now show a "gallery is at its 20-photo limit" message in place of the upload form once a gallery is full, computed from the already-fetched image list rather than a second query.
  3. **The public vendor profile (`/vendors/[id]/page.tsx`) now actually uses the gallery**, rather than a flat list of thumbnails below the description. With no photos, the description renders in a larger "banner" treatment (a gradient `Card`, a quote-mark icon, bigger type) standing in for a hero image. With photos, a new full-bleed swipeable carousel (`vendor-gallery-carousel.tsx`, native CSS scroll-snap plus dot indicators driven by a plain `onScroll` handler — no gesture library) replaces the old grid — the specific style (full-bleed carousel, over a plain grid/hero-and-thumbnails/horizontal-scroll alternative) was Andre's own choice among a few options presented for this decision, since it's a visual/product call rather than a technical one.

  **A real bug caught by the project's own "check the browser console" verification habit, not by reading the diff**: the first version of both gallery upload forms wrapped `useActionState`'s returned `formAction` dispatcher in a second, outer async function (to run compression first, then call `formAction(formData)` manually) — it worked, but logged a React warning every time: *"An async function with useActionState was called outside of a transition."* The fix was to move the compression step *inside* the function passed to `useActionState` itself (so it runs as part of the transition React already sets up for a real form submission), and pass the returned `formAction` directly to the `<form>`'s `action` prop with no wrapper at all. The lesson generalizes: never call a `useActionState` dispatcher manually from inside another function — any pre-processing of `formData` belongs inside the state-updater function passed to the hook.

  **A separate build-breaking mistake, caught immediately by `npm run build`**: exporting the `MAX_GALLERY_IMAGES` constant directly from a `"use server"` actions file broke the *entire* file ("the module has no exports at all") — a `"use server"` file may only export async functions, since every export is treated as a callable Server Function. Fixed by moving the constant to a plain shared module, `src/lib/gallery-limits.ts`, imported by both the actions file (to enforce it) and the page component (to reflect it in the UI). See `teachAndre/03-server-actions-and-client-state.md` for the fuller writeup.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass against the local stack: uploaded a real 7.2MB synthetic test photo through the vendor dashboard's actual file input and confirmed via a direct Postgres/Storage query that the object that landed in the `vendor-gallery` bucket was ~312KB, not the original size; seeded both a vendor's and an event's gallery to exactly 20 rows directly in Postgres (not by uploading 20 real files) and confirmed, reloaded through the real page, that the upload form is replaced by the limit message on both dashboards; visited the public profile of the now multi-photo test vendor and confirmed the carousel renders with dot indicators, and a separate unverified vendor with a description but no photos and confirmed the gradient banner renders instead. All seeded/duplicate test rows and their orphaned Storage objects were removed afterward via the real Storage API (not a raw table `DELETE`), leaving one genuine compressed test photo in the vendor test account's gallery as a working demonstration rather than restoring it to empty.

  **A second, genuinely new LAN-testing gap found immediately afterward, this time by Andre's own phone**: a real photo uploaded from the vendor dashboard over the LAN showed as a broken image on the gallery grid. Root cause was the same category of issue as mobile testing round 1's `allowedDevOrigins` gap, but on the Supabase side instead of the Next.js side: `NEXT_PUBLIC_SUPABASE_URL` was `http://127.0.0.1:54321`, and `getPublicUrl()`/`createSignedUrl()` bake whatever this value is directly into the URL handed to the browser for an `<img src>` — unlike every other Supabase call in this app, which runs inside a Server Action or Server Component on the dev machine itself, where `127.0.0.1` correctly means "this machine." A phone's browser resolves `127.0.0.1` to *itself*, not the dev machine, so the request never left the phone. Confirmed by fetching the exact same broken URL's path against the LAN IP instead and getting a real `200`. Fixed by pointing `NEXT_PUBLIC_SUPABASE_URL` at the same LAN IP `next.config.ts`'s `allowedDevOrigins` already uses (`http://192.168.31.140:54321` at time of writing) rather than `127.0.0.1` — Supabase's local Kong gateway already binds `0.0.0.0:54321`, so this needed no Supabase-side config change, only the env var. Since this is a `NEXT_PUBLIC_` variable, it's inlined into the client bundle at dev-server-start time, not read per-request — it takes a full `npm run dev` restart to pick up, hot-reload alone won't. **The rule of thumb this and the `allowedDevOrigins` gap both point at**: any time this project is tested over a LAN instead of `localhost`, re-check every `NEXT_PUBLIC_*`/dev-server-origin setting that a browser (not the Node server) reads directly — `localhost`/`127.0.0.1` values are correct from the dev machine's own browser and silently wrong from any other device's.

- **2026-09-14 (A real "already invited?" scare traced to a labeling gap, not a bug; date/name sort toggles; a per-device public-events checkbox) — three small, independent fixes from one afternoon of continued use.**
  1. **A brand-new signup ("Andre 1") appeared to already have event invitations** — alarming if true, since `event_collaborators` invites are meant to only ever be created by an existing member explicitly inviting a known email (`inviteCollaborator`, resolved via `findUserByEmail`, never automatic). Checked directly against Postgres before assuming anything: the new account had zero `event_collaborators` rows. What was actually happening: Home's "Upcoming Events" section (public, published events — the same content guests see) sat directly beneath the genuinely personal "Upcoming Tasks"/"Upcoming Payments" sections added earlier this same day, with no distinguishing label, so the whole feed read as personalized when it wasn't. Relabeled to "Discover Public Events" with a one-line clarifier ("Open to everyone on Gather — not your own events. Find those under My Events.") — copy-only, no logic changed, since there was no actual bug to fix.
  2. **Sort toggles for My Events (by date) and Vendors (by name)** — both lists already had a fixed default order (`start_at` ascending, `name` ascending) from the day each was built, but with no way to see or change it. Rather than assume a full sort-by-field-and-direction menu was wanted, asked first and built the smaller, explicitly-requested thing: a single direction-toggle pill per page ("Date ↑ Soonest first" / "↓ Latest first", "Name A→Z" / "Z→A"), implemented as plain GET-param links (`?sort=desc`) — the same server-rendered, no-client-JS filtering pattern the Vendors page's search/category controls already use, so no new client component was needed for either toggle. Vendors' toggle had one extra piece of care the Events one didn't: an existing search query or category filter must survive toggling sort (and vice versa), so the three ad-hoc `href` string-builders already on that page were consolidated into one `buildHref(overrides)` helper that always carries all three params forward, only overriding the one actually being changed.
  3. **A per-device checkbox to hide the "Discover Public Events" section entirely** (`src/components/public-events-section.tsx`) — for a planner who only ever cares about their own events, the public directory on their own Home page is pure clutter. Deliberately built as a client-only `localStorage` preference, not a new column on `notification_preferences`: this is a single browser's display choice, not account data that needs to sync across devices or be readable server-side, so a DB round-trip (and the migration it would need) would be more machinery than the actual ask called for — the same "does this need to be a per-request store or can it just be a plain client-side one" judgment call this project has made before, just landing on the simpler side this time since nothing here is seeded from the server.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass: toggling both sort pills actually reverses the rendered order (compared the vendor name list before/after byte-for-byte, confirmed it's a true reverse, not a no-op); confirmed a category filter link still carries `sort=desc` after toggling sort first; confirmed the public-events checkbox hides the section, persists across a full page reload (real `localStorage`, not just component state), and re-showing it works too — zero console errors throughout.

- **2026-09-14 (My Events was leaking every public event on the platform to every signed-in user) — a real bug, found the moment the previous entry's "no bug, just a labeling gap" diagnosis got re-tested by a second brand-new signup.** The first "am I already invited?" report (above) genuinely had zero matching `event_collaborators` rows — but a second look, on a fresh account, showed several unrelated test events ("Confetti Test Party" ×3, "Feedback Fix Party") on `/events` labeled "Invited," which a direct Postgres check confirmed had no collaborator row either. Root cause: `events_select_public` (the RLS policy that lets Home's guest-facing directory work) is granted `to: [anonRole, authenticatedRole]` — it applies to every signed-in user too, not only guests, and Postgres combines multiple permissive SELECT policies with OR. `/events/page.tsx`'s query relied on RLS alone to scope the result set ("no explicit owner_id filter needed," per its own comment) — true for `events_select_owner_or_collaborator`, false once `events_select_public`'s independent, unconditional grant is added into the mix. The net effect: **any** published + public event in the entire system showed up on **everyone's** My Events, mislabeled "Invited" since the UI's badge logic only distinguished "owned" from "not owned." Confirmed on the admin test account directly against Postgres before fixing anything: it owned exactly 5 events, and all 5 of the platform's public events belonged to someone else — yet `/events` was rendering all 10.

  **The fix**: `/events/page.tsx` now queries the user's real `event_collaborators` rows first, then filters the `events` query explicitly with `.or("owner_id.eq.<uid>,id.in.(<collaborator ids>)")` — no longer trusting RLS's necessarily-broader OR to also mean "belongs to me." A new **"Include public events"** toggle (styled and wired exactly like the sort-direction pills added in the same round — a plain GET-param link, no client JS) opts back into the wider directory on request, adding `and(status.eq.published,visibility.eq.public)` to that same `.or()` clause — and any event shown only because of that clause now gets a third badge, **"Public"** (not "Invited"), so this exact confusion can't recur even with the feature turned on. This was the user's own follow-up ask once the mislabeling was found: keep Home's "Discover Public Events" section as already built, but add the equivalent opt-in control to My Events instead of it being a permanent, unlabeled leak.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass against the admin test account (5 owned events, 0 collaborator rows, 5 unrelated public events confirmed in Postgres beforehand): the default `/events` view now shows exactly the 5 owned events, zero leakage; toggling "Include public events" grows the list to all 10, with the 5 formerly-leaking events now rendering the new "Public" badge instead of "Invited"; the sort-direction param and the new toggle's param combine correctly in the URL without clobbering each other. Zero console errors.

- **2026-09-14 (Profile's notification-preferences dropdown appeared to revert to "1 week before" on every save) — two independent bugs stacked on the same symptom, the second one only found because the fix for the first wasn't enough on its own.** Reported as "I change the dropdown, hit save, it jumps back" — checked the database directly before touching any code, since this exact wording ("jumps back to X") could mean either a genuine failed write or a display-only staleness bug, and those need opposite fixes.
  1. **The write itself was correct on the very first save** (confirmed straight against Postgres). What was stale was the *page*: `saveNotificationPreferences` was a plain `<form action={serverFunction}>` relying on `revalidatePath("/profile")` alone to refresh the already-mounted page — the exact same category of bug this project's own `AcceptQuoteForm` hit and fixed earlier this session (documented in the full-project-review entry above): `revalidatePath` inside a Server Action reliably invalidates the server-side cache, but doesn't reliably repaint a page the user is already sitting on. Fixed the same proven way: converted the plain form into `notification-preferences-form.tsx`, a client component using `useActionState` + a `useEffect` that calls `router.refresh()` once the action's returned state confirms success — never bundling the refresh into the action's own response, which is what caused the original Phase-4-era version of this exact bug (documented in `teachAndre/04-derived-state-and-router-cache.md`) to clobber itself.
  2. **That alone wasn't enough** — re-tested after the first fix and the dropdown *still* showed the pre-save option immediately after saving (only a hard reload showed the truly correct value). Root cause was a second, more specific React quirk: a `<select>`'s `defaultValue` (like any uncontrolled element's) is only applied when the DOM node first mounts, never re-applied on a later update — `router.refresh()` genuinely does fetch the fresh saved value into this component's props, but without something forcing React to treat the element as new, the already-mounted `<select>` just keeps showing whatever it already had. Fixed with `key={upcomingWindow}` on the `<select>` itself — keying an uncontrolled element on the very value it's meant to reflect is the standard React pattern for "reset this when an external value changes," forcing a real remount (and therefore a fresh `defaultValue` application) exactly when the saved value actually changes, and never otherwise.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass reproducing the exact reported flow twice, in both directions (one week before → on the day, and back): captured the dropdown's value on load, changed it, saved, and checked the value with *no reload* — confirmed broken (showed the pre-save option) after fix #1 alone, confirmed correct after fix #2 was added, then confirmed a hard reload still agreed in both cases. The database write itself was correct in every single run, from the very first test — this was never a data-integrity bug, only ever a client-repaint one, which is exactly why checking Postgres directly before writing any fix mattered here.

- **2026-09-14 (Edit Profile + Edit Business Details — closing a real gap where onboarding was a one-way door) — "I can do it on sign up but never again."** Both `completePlannerProfile` (Planner name/phone) and `registerVendorBusiness` (vendor name/category/description/phone/website) had always been create-only, one-time forms — there was never a path back to either afterward, confirmed by grepping the whole codebase for any other write to these columns before building anything (there wasn't one). Built as two small, separate pages rather than one shared component, since the two forms edit unrelated tables under unrelated RLS policies and share no fields:
  1. **`/profile/edit`** — reuses `completePlannerProfile`'s exact zod schema and field set (name/phone), now callable again from a new "Edit Profile" link next to "Signed in as ..." on `/profile`. Follows `updateEvent`'s established idiom for turning an RLS-filtered no-op into a real error: `.update(...).eq("id", user.id).select("id").single()`, where `.single()` throws on the zero-row case `profiles_update_own` would silently produce for an unauthorized write (which can't actually happen here since it's always the caller's own row, but keeps the pattern consistent everywhere it's used). `revalidatePath("/", "layout")` before the redirect, for the same reason onboarding's own save needed it — a display-name change doesn't mutate a cookie, so nothing else forces the root layout (and this same Profile page's own "Signed in as" line) off Next's client Router Cache.
  2. **`/vendor/[vendorId]/edit`** — reuses `registerVendorBusiness`'s exact zod schema and field set, now callable again from a new "Edit Business" link next to "Manage Team" on the vendor dashboard (shown only when `canQuote`, the same owner/manager derivation the dashboard's other edit-like affordances already use). The page's own access check deliberately mirrors `vendors_update_creator_stub_or_owner_manager`'s full OR condition, not just the team-role half of it — `(unclaimed AND created_by = me) OR owner/manager` — even though the dashboard link only ever surfaces the owner/manager path (an unclaimed stub's creator generally isn't a team member yet, so has no dashboard to click the link from at all), so that a stub creator hitting the URL directly, if that path is ever wired up elsewhere later, gets a working form rather than a silent RLS-filtered save. Social links stayed out of scope here on purpose — `vendor_social_links` has no UPDATE policy at all by design (delete-and-re-add only), so there was nothing to add to this form for them; the existing add/remove UI on the dashboard is already the whole feature.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass: changed the admin test account's display name through the real form, confirmed `/profile` immediately showed the new name with no reload (then changed it back, to leave the fixture account as found); changed the vendor test account's category through the real form, confirmed it persisted across a fresh navigation to the edit page (then reverted); confirmed a signed-in planner with no relationship to that vendor gets a genuine 404 hitting `/vendor/{id}/edit` directly, and a signed-out request to `/profile/edit` gets redirected to `/login`. Zero console errors throughout.

- **2026-09-14 (iOS Safari kept zooming in on navigation, eventually opening a horizontal scrollbar) — reported from a real iPhone, not reproducible in desktop Chrome, which is itself a clue.** The symptom ("keeps zooming in when I navigate around, then it creates a horizontal scrollbar") is the textbook description of a well-known, specific iOS Safari behavior, not a layout bug: Safari auto-zooms the whole page the instant a focused text input's *computed* font size is under 16px, to keep the text legible. Checked the actual rendered viewport meta tag first, to rule out the more common cause of this class of bug — `curl`'d the homepage and confirmed `width=device-width, initial-scale=1, viewport-fit=cover` was already correctly present (Next.js's `Viewport` export does merge a partial object like this project's `{ viewportFit: "cover" }` with its own framework defaults, contrary to how a partial `metadata` object behaves — worth knowing, since the more common version of this bug really is a missing/broken viewport tag). With that ruled out, checked every text-entry element's actual computed font size instead: the shared `Input` component and every raw `<select>` in the app (event form, notification preferences, RSVP editor, invite forms, and others) were all set to Tailwind's `text-sm`, which is 14px — under the threshold. On a client-routed Next.js app, that auto-zoom doesn't reset between page navigations the way it would after a real full-page reload, so it can compound across a session: zoom in on the first focused input, navigate, the zoom level carries over, and the fixed-width mobile shell (`max-w-sm`, sized for a 1× viewport) eventually renders wider than the effectively-smaller zoomed-in viewport, which is what actually produces the horizontal scrollbar.
  - **The fix**: one global CSS rule in `globals.css` — `input, select, textarea { font-size: 16px; }` — deliberately written as plain, un-layered CSS rather than inside `@layer base` (the opposite choice from, and for the opposite reason as, the `a`/`button` defaults a few lines below it in the same file): Tailwind v4 gives un-layered CSS higher priority than any of its own utility classes regardless of specificity, which is exactly what's needed here to reliably beat the `text-sm` utility already present on every one of these elements, rather than losing to it the normal way `@layer base` rules do.
  - **A defensive second layer**, since the underlying report was specifically about a horizontal scrollbar appearing: `overflow-x: hidden` on `html`/`body`, added to the existing `@layer base` block. This isn't the fix for the zoom itself — it's a safety net so that if anything, on any device, for any reason, ever renders wider than the viewport again, it clips instead of opening a scrollbar the mobile shell was never designed to have.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass confirmed every checked text input (login's email field, the vendors search box) and a raw `<select>` (notification preferences) now compute to `16px`, up from `14px`; confirmed Home has no horizontal overflow (`scrollWidth` no longer exceeds `clientWidth`); and a visual screenshot at a real iPhone 13 viewport size confirmed the slightly larger inputs still fit the existing login form cleanly, no layout breakage from the size bump. The zoom behavior itself is iOS-Safari-specific and can't be reproduced in Chromium, so this fix rests on removing the documented trigger condition rather than reproducing the bug end to end — flagged here in case Andre wants to re-confirm on the actual iPhone that reported it.

- **2026-09-14 (Attendees: RSVP filter, edit-after-add, a capacity/total header, and a collapsible breakdown) — four related asks in one message, all scoped to the existing `/events/[id]/attendees` page.** What shipped:
  1. **A status filter row** (All / Attending / Maybe / Declined / No response) — the same plain GET-param `Link` pill pattern already used on Vendors/My Events (`?status=attending`), not a new client component. The full, unfiltered attendee list is still fetched in one query regardless of the active filter — filtering happens in JS against that one result — specifically so the capacity header and breakdown counts (below) always reflect the true event-wide numbers, never just whatever the current filter happens to be narrowed to.
  2. **Edit-after-add**: previously an attendee's name/email/guest count could only ever be set once, when `AddAttendeeForm` first created them — only their RSVP status had a way back to it (`RsvpSelect`). New `updateAttendeeDetails` action (same shape/validation as `addAttendee`, same "check the returned rows, not just `{error}`" idiom `updateAttendeeRsvp` already established) plus a new `AttendeeRow` client component that replaces each attendee's static card with an inline edit toggle — tap "Edit," the row becomes a small form pre-filled with that attendee's current values, Save or Cancel returns to the display view. Deliberately sidesteps the `<select>`-`defaultValue` staleness bug fixed earlier this session in Profile's notification preferences: the edit form's inputs only exist in the DOM while `editing` is true, freshly mounted each time it's opened and fully unmounted on Cancel/success, so there's never an already-mounted uncontrolled input that needs to react to a prop changing underneath it — on success it just calls `router.refresh()` and falls back to displaying the (now-fresh) `attendee` prop, the same proven pattern as `AcceptQuoteForm`.
  3. **A capacity-aware total at the top of the page**: `"{count} of {capacity} invited"` when the event has a capacity set (events.capacity was already a column, just never surfaced here), or plain `"{count} invited"` when it doesn't — computed from the same always-unfiltered attendee count as the breakdown, not affected by the status filter.
  4. **A collapsible breakdown** ("Total invited" plus a count per RSVP status) using a native `<details>`/`<summary>` element — the first use of either in this codebase — rather than a new client component with its own open/closed state, since the browser's own built-in disclosure widget already does exactly what was asked ("a collapsible details page") with zero JS and no accessibility gaps to account for.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass against a real fixture event (capacity 5, one existing attendee): confirmed the header read "1 of 5 invited"; added a second attendee through the real form and confirmed it became "2 of 5 invited" with the breakdown's "Total invited" and per-status counts updating to match; filtered by "No response" and confirmed both attendees showed, changed the new one's RSVP to Attending via `RsvpSelect`, then confirmed the "Attending" filter now showed only that one; edited that same attendee's name and guest count through the new inline form and confirmed the change displayed immediately with no reload. All test-added attendee rows were removed afterward, restoring the fixture event to its original single-attendee state. Zero console errors throughout.

- **2026-09-14 (RSVP control redesign: 3 tap icons instead of a native `<select>`) — same-day follow-up once the shipped attendees page was actually looked at.** The native `<select>` (`RsvpSelect`) rendered as a wide, all-caps, browser-chrome-heavy control that read as out of place next to a plain "Edit" text link — a real, reasonable design complaint, not a functional one. Presented 4 concrete redesign directions (a restyled compact select, moving RSVP into the edit form as a read-only badge, an icon quick-toggle, and a kebab-menu declutter) with ASCII previews rather than guessing; the icon quick-toggle was picked. `rsvp-select.tsx` → `rsvp-toggle.tsx`: 3 small circular icon buttons (`Check`/`HelpCircle`/`X` from `lucide-react`, already the icon set used everywhere else in the app) for Attending/Maybe/Declined. "No response" isn't a 4th tappable option — it's simply what showing with none of the 3 pressed already means, and tapping the currently-active icon again clears it back to that state, so the redesign still fully covers the same 4-value enum with 3 controls, not 4. Kept the exact same controlled-state-plus-`useTransition` shape (and the same `updateAttendeeRsvp` action) the select already had — this was a presentation-layer change only, not a behavior change, so the existing "revert on failure, toast on error" logic needed zero modification.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass: confirmed the button's own `aria-pressed` state and background color class both flip correctly on tap (`bg-success-soft` for Attending), confirmed tapping the same active icon again reverts `aria-pressed` back to `false` (the "no response" state), and confirmed that reverted state survives a full page reload — i.e., a real, persisted round-trip through `updateAttendeeRsvp`, not just local UI state. Zero console errors; no attendee data required cleanup since the test round-tripped back to the original "no response" state before finishing.

- **2026-09-14 (Event Types, Service Categories, and Suggested Vendors — the biggest single feature this session, run through full plan-mode design first) — `events.event_type` had been a free-text field since Phase 5, captured on the create/edit form but never once displayed anywhere in the app. The ask: make it a real admin-managed dropdown (with an "Other" + free-text fallback for anything not yet listed), specifically so the app could suggest vendors to a planner based on what kind of event they're planning.** The user's own mid-design correction shaped the final architecture: event types (Wedding, Birthday, Corporate) and vendor service categories (Music, Cakes, Photography, Catering) are two different taxonomies, not one shared list — a single wedding needs several service categories at once. Went through full plan mode (two Explore passes, one Plan pass, a written plan file, explicit approval) before writing any code, given the number of real architectural decisions involved.

  **What shipped**:
  1. Three new tables — `event_types` and `service_categories` (identical shape: `id, name` unique, `is_active`, `created_at`, public `select`, admin-service-role-only writes), and `event_type_service_categories`, this project's first true many-to-many junction table (a composite primary key on the pair, not a surrogate id, since the row carries no payload beyond the pair itself). Two new nullable, `onDelete: "set null"` columns — `events.event_type_id` and `vendor_services.category_id` — added *alongside* the existing `events.event_type` text column rather than replacing it: a real picked type copies its current name into `event_type` (so every existing display path keeps working with zero joins) and sets the id; picking "Other" leaves the id null and stores the free text typed in instead. No raw SQL / `supabase/migrations/` file needed — every new policy is a plain public-read `using: sql\`true\``, nothing here crosses schemas the way `is_admin()`/`is_vendor_team_member()` do.
  2. **`/admin/event-types`** and **`/admin/service-categories`** — add (real, `useActionState`-validated data entry, duplicate names caught with a friendly message before the unique index would) and deactivate/reactivate (fire-and-forget, matching `/admin/vendors/claims`'s existing row-toggle idiom) for each list. **`/admin/event-types/[id]`** is the mapping editor — every service category as a per-row Link/Unlink fire-and-forget button (not a checkbox-grid-with-batch-save, which would be new, unprecedented complexity this scale doesn't need), which is what "Suggested Vendors" actually reads from.
  3. The event create/edit form (`event-form.tsx`) replaced its free-text `<Input>` with a dropdown (active types + a literal "Other" option) and a free-text field that only appears when "Other" is picked — the one controlled field in an otherwise all-`defaultValue` form, since a second field's visibility depends on it. A shared `resolveEventType()` helper (not itself a Server Action, just a plain module both `createEvent` and `updateEvent` import) does a real DB lookup for a genuinely-picked type rather than trusting the dropdown — closing the race where an admin deactivates a type between page load and submit, and supplying the current name for the denormalized `event_type` column in the same round trip.
  4. Vendor services gained a required category tag (`add-service-form.tsx` + `addService`), shown on the dashboard as a small label (or "Uncategorized" for the pre-existing rows this doesn't backfill — the same nullable-additive-column tolerance already extended to `vendor_gallery_images.caption`).
  5. **Suggested Vendors** on the event's own vendors page: if the event has a real type, look up its mapped category ids, find vendors with a service in any of them, exclude vendors already associated, cap at 8 — one flat query plus in-app dedupe, no RPC or background job. Visible only to `access.isEditor` (matching every other add-affordance on this page), with an "Add" button that reuses `associateWithEvent` from the vendor-side directly rather than a new action file (wrapped in a tiny inline Server Action to discard its `{error?}` return, since a plain `<form action>` needs `void`). This also closed a real, separate structural gap: the page's only "add a vendor" link previously lived *inside* the empty-state branch and vanished entirely once one vendor existed — it now renders unconditionally as "Browse all vendors" below both lists.

  **A real bug found by the first Playwright run, not by reading the diff**: creating an event with a real (non-"Other") type failed validation with a bare "Invalid input" — the free-text `eventTypeOther` input, conditionally rendered only when "Other" is selected, simply doesn't exist in the DOM the rest of the time, so `formData.get("eventTypeOther")` returns `null`, not `undefined`. Every other optional field in this same zod schema works fine with `formData.get(...)` because those inputs are always present in the DOM (just empty), returning `""`; this was the first field in the codebase whose presence in the form is itself conditional. `z.string().optional()` only tolerates `undefined`, not `null`, so the fix was normalizing at the read site: `formData.get("eventTypeOther") || ""` in both `createEvent` and `updateEvent`. Worth remembering as a general rule for any future conditionally-rendered form field: `formData.get()` on a field that simply isn't in the form returns `null`, which is a different (and differently-invalid) value than an empty-but-present input's `""`.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean, `npm run db:generate` + `npm run db:migrate` applied against the local stack and spot-checked directly in Postgres. A real, full Playwright walkthrough matching the plan's own verification script exactly: as admin, created an event type and two service categories and linked one to the type; as a planner, created an event selecting that type (confirmed the right `event_type`/`event_type_id` landed) and separately one using "Other" with custom text (confirmed `event_type_id` stayed null and the edit page correctly preselected "Other" with the text prefilled); as a vendor, added a service tagged with the matching category (confirmed the category tag rendered on the dashboard); back as the planner, confirmed the vendor appeared under "Suggested Vendors" on the event's vendors page, clicked Add, confirmed it moved into "your vendors" and the suggestion disappeared on the next load. Zero console errors throughout. All test-created event types, categories, mappings, services, and events (plus the `event_vendors` row the Add button created) were removed afterward via direct SQL, respecting the FK order (child rows before parents) rather than leaving orphaned fixture data.

- **2026-09-14 (Attendee headcount didn't count additional guests) — a real bug in the capacity total shipped earlier the same day.** The "X of {capacity} invited" header and the "Total invited" line in the collapsible breakdown were both computed as `attendees.length` — a count of *rows*, not people. An attendee bringing 2 extra guests was one row but 3 actual people against the event's capacity, so the total silently undercounted any event with guests recorded. Fixed by summing `1 + guest_count` across every attendee for both numbers (`src/app/events/[id]/attendees/page.tsx`), rather than a plain row count. Deliberately left the per-status breakdown lines (Attending/Maybe/Declined/No response) as attendee-record counts, not headcount — those answer "how many people responded," a different question from "how many bodies show up" — and added a one-line caption clarifying the two use different units, since without it a reader could reasonably read the mismatch between "Total invited" and the sum of the per-status lines below it as a new bug rather than an intentional distinction.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright check against the same fixture event used in the original attendees-feature verification (one attendee, "Monique," with `guest_count: 1`, capacity 5): confirmed the header now reads "2 of 5 invited" (previously "1 of 5") and the breakdown's "Total invited" line matches at 2.

- **2026-09-14 (Seeded a starter Event Types/Service Categories taxonomy; admin console can now see and promote "Other" free text) — two follow-ups to the same-day Event Types feature, both aimed at making the taxonomy actually usable before real planners start typing into it.**
  1. **Seed data**: both lookup tables (and their mapping) were empty since nothing had populated them through the admin UI yet, and clicking "Add" dozens of times by hand isn't a reasonable way to bootstrap a starter list. Ran a one-off Node script (using the `postgres` package directly, the same established pattern this project already uses for one-time data work — not a checked-in migration, since this is seed *data*, not schema) inserting 20 common event types (Wedding, Birthday Party, Corporate Event, Baby Shower, Bar/Bat Mitzvah, Quinceañera, Conference, Fundraiser/Gala, ...), 20 service categories (Photography, Catering, Cakes & Desserts, Music/DJ, Florals, Lighting & AV, Officiant, ...), and 144 event-type-to-category mappings reflecting which categories are typically relevant to which event type (e.g. Wedding maps to 18 categories, Team Building to 4). Explicitly a starting point for Andre to review and tune through the admin console's existing add/deactivate/link/unlink UI, not a final taxonomy — nothing about this is hardcoded or hard to change later.
  2. **"Other" free-text visibility**: previously, once a planner typed a custom event type, an admin had no way to see what people were actually typing, which was the whole point of the "Other" escape hatch existing in the first place — deciding whether something typed often enough deserves to become a real listed type. `/admin/event-types` now has an **"Other" Free Text** section: every event with `event_type_id IS NULL` and a non-null `event_type`, grouped by exact text match (aggregated in application code — Supabase-js has no `GROUP BY`, and the expected row count for an admin-facing report like this is small enough that a raw-SQL view/RPC would be more machinery than the problem needs), sorted by how many events used that exact text. Each group gets an **"Add as type"** button (`promoteOtherType`, a new fire-and-forget admin action) that both creates the real `event_types` row (or reuses one if a same-named type already exists) *and* backfills every matching event's `event_type_id` — closing the loop so promoting a common "Other" entry doesn't leave the events that already used it looking disconnected from the type they clearly meant.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean; the seed script's insert counts (20/20/144) and a sample mapping were spot-checked directly against Postgres. A real Playwright pass: created two separate events with the identical custom "Other" text, confirmed the admin page grouped them as one entry reading "2 events," clicked "Add as type," confirmed it now appears in the real Event Types list above and disappears from the "Other" section, and confirmed directly in Postgres that both events' `event_type_id` were actually backfilled to the newly created type — not just removed from the report. Zero console errors; all test-created events/types removed afterward.

- **2026-09-14 (Attendee breakdown: per-status counts didn't include guests either) — same-day follow-up to the total-headcount fix above, reported the moment it made the *breakdown* look wrong instead.** That earlier fix deliberately left the per-status lines (Attending/Maybe/Declined/No response) as plain attendee-record counts, reasoning they answered a different question ("how many people responded") than the headline total ("how many bodies show up") — with a caption added specifically to explain why the two didn't sum to the same thing. In practice this read as confusing, not clarifying: seeing "Total invited: 2" next to "Attending: 1" looks like a bug regardless of a footnote explaining it isn't one. Fixed by applying the exact same `1 + guest_count` headcount rule to the per-status counts (`src/app/events/[id]/attendees/page.tsx`) that `totalHeadcount` already used, so the breakdown now actually sums to the header number, and simplified the caption to just "Counts include each attendee's additional guests" now that there's only one consistent rule to explain, not two different ones to distinguish.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright check against the same fixture attendee ("Monique," `rsvp_status: attending`, `guest_count: 1`): confirmed "Attending" in the breakdown now reads 2, matching "Total invited," instead of the previous 1.

- **2026-09-14 (Budget: line items, vendor confirmation, and a spend warning — this session's second full plan-mode feature, and its first with a real design conversation before the plan itself).** Before writing a plan, four architectural forks were put to the user directly rather than assumed: (1) one vendor per budget line vs. comparing multiple vendor candidates per line — **one vendor per line** won, matching how every other vendor booking already works; (2) what "spend" means for the budget total/warning — **committed spend** (the sum of linked payment plans' totals) won over raw per-line estimates or amounts actually paid; (3) how a payment plan attaches to a budget line, given plans already require picking a vendor booking first — **an additive optional tag** on the existing creation flow won over restructuring that flow to start from Budget instead; (4) the "approaching budget" warning threshold — **configurable per event**, not a fixed app-wide percentage. Research before the plan also surfaced a real, ready-to-use fact: `event_vendors.confirmed` has existed since Phase 7 but was **entirely dead** — defaulting to `false`, displayed as a read-only badge in two places, written to by nothing anywhere in the app, even though its own RLS policy already permitted the write. Marking a vendor confirmed/pending was a real feature gap, not a surfacing job.

  **What shipped**:
  1. Three additive schema changes, no existing column repurposed: `events.budget_total` + `events.budget_warning_percent` (both nullable, same "optional, no forced default" treatment as the existing `capacity` column — a null warning percent falls back to a single exported `DEFAULT_BUDGET_WARNING_PERCENT` constant, not a stored 90 on every event that never set one); a new table `budget_items` (this project's planner-only line-item breakdown — `label`, `budgeted_amount`, an optional `category_id` reusing the same `service_categories` taxonomy vendor services already use, and an optional `event_vendor_id` for the one linked booking), RLS mirroring `event_tasks`/`event_attendees` exactly (owner-or-collaborator select, owner-or-editor write, no vendor-side branch — same spirit as the already-documented "payment plans are planner-only" rule); and `payment_plans.budget_item_id`, the additive tag from decision 3.
  2. **`setVendorConfirmed`** (`.../vendors/[eventVendorId]/actions.ts`) — the `declineQuote` idiom (plain fire-and-forget action, `revalidatePath`, no `useActionState`) for what's fundamentally a two-state toggle. The event-vendor detail page's own status line is now itself the toggle button for an editor, or a plain read-only badge for anyone else.
  3. **`/events/[id]/budget`** — a new page gated identically to the existing Payments page (view: owner or any collaborator; mutate: owner or editor). Header shows committed spend against the total (when set) with a live percentage, an amber "Approaching budget" or red "Over budget" banner once committed spend crosses the threshold, an add-item form, and a `budget-item-row.tsx` per line (mirroring `attendee-row.tsx`'s inline-edit-toggle shape exactly, for the same defaultValue-staleness-avoidance reason established earlier this session) — each row shows its category tag, links to one of the event's already-associated vendors via a plain `<select>` (reusing the existing association rather than a new vendor-search UI), and reflects that vendor's live Confirmed/Pending status plus any payment plan tagged to it.
  4. **Committed spend is computed from every payment plan belonging to the event's vendors, not just ones tagged to a budget item** — a deliberate choice matching decision 2: a plan created the old way, straight from a vendor's own Payments page with no budget line ever involved, must still count toward the warning, or the number would understate real spend for anyone who doesn't visit Budget for every plan. The per-line display, by contrast, only shows a plan if it's actually tagged to that specific line — two different, both-correct views of the same underlying data, described explicitly in the page's own top-of-file comment so the distinction doesn't read as an inconsistency later.
  5. `create-plan-form.tsx` gained one new optional field — a `<select>` of the event's existing budget items, only rendered when any exist — and `createPaymentPlan`'s schema/insert gained the matching `budget_item_id`, with zero change to the plan-creation flow's existing required fields.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean; the migration was applied and spot-checked directly in Postgres. A real, full Playwright walkthrough: created an event with a R1,000 budget and a 50% warning threshold; associated a vendor with it; added a "Catering" budget item (R800, tagged to the Catering service category) and linked the vendor to it; marked that vendor Confirmed on its own detail page and confirmed the budget line's badge updated to match with no manual refresh; created a R600 payment plan for that vendor tagged to the Catering line and confirmed both the line's own "R600,00 committed" and the page-level "R600,00 committed of R1,000,00 budget (60%)" — with the amber warning banner now showing, since 60% ≥ the 50% threshold; then lowered the event's budget to R500 (committed unchanged) and confirmed the banner correctly switched to the red "Over budget" state at 120%. All test-created events/vendors/plans/budget items were removed afterward via direct SQL, respecting FK order (installments → plans/quotes → budget items/associations → the event itself). Zero console errors throughout.

- **2026-09-14 (Budget follow-up: no way to create a plan straight from a linked-but-unpaid budget line, and no visibility into a vendor's budget links from its own detail page) — same-day feedback after actually using the shipped feature.** Two real gaps, both found from a live screenshot of a genuine test fixture, not hypothetical:
  1. A budget line with a vendor linked but no payment plan yet said "No payment plan yet" with no way to act on it — fixed by turning that text into a "Create a payment plan" link straight to that vendor's Payments page, carrying the budget item id as a `?budgetItem=` query param that `CreatePlanForm` now accepts as a `defaultBudgetItemId` to preselect.
  2. Verifying that link against a **real** fixture event surfaced a genuine edge case the original design missed entirely: that event's linked vendor already had a payment plan from *before* this feature existed, created the old way with no budget-item tag. The new "Create a payment plan" link would have landed on that already-existing plan showing no create form at all, silently failing to do what the link promised. Fixed with a second new action, `setPlanBudgetItem` — an "attach to a budget line" `<select>` now shown on every existing plan's own page (not just at creation time), so a plan created before or without ever visiting Budget can still be tagged (or re-tagged, or cleared) after the fact. `budgetItems` is now fetched on the Payments page unconditionally, not only when no plan exists yet, since both the creation form and this new control need it.
  3. The event-vendor detail page previously gave zero indication that a budget line pointed back at that exact booking — fixed by querying `budget_items` for `event_vendor_id = this booking` and showing a small "Linked budget line(s)" card (plural-aware, since nothing stops the same vendor being linked from more than one line) linking through to the Budget page.

  **How this was actually verified**: `npm run build` and `npm test` (16/16) pass clean. A real Playwright pass against the exact fixture from the screenshot that prompted this (`Test Andre` event, `Flow Test Vendor`, a pre-existing untagged R1,000 plan): confirmed the budget line's "Create a payment plan" link carries the budget item id, confirmed following it correctly lands on the *existing* plan (not a blank create form) with the new attach-select visible, tagged it to "Catering," and confirmed the budget page immediately reflected "R1 000,00 committed" both at the line and page level with the "create a plan" link now gone — then confirmed the vendor's own detail page now shows "Linked budget line(s): Catering." Zero console errors; the fixture was left in this now-correctly-tagged state rather than reverted, since it reflects the real, intended end state the screenshot's own workflow was heading toward.
