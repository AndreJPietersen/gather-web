// The "approaching budget" warning threshold, used whenever an event
// hasn't set its own budgetWarningPercent — one shared constant rather
// than hardcoding 90 in both the budget page's fallback and the event
// form's hint text.
export const DEFAULT_BUDGET_WARNING_PERCENT = 90;
